name: Pytest Focused Temporary

on:
  push:
    branches: [spec/075-process-kernel-1]

jobs:
  pytest-focused:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt
      - name: Install dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
      - name: Capture focused pytest output
        working-directory: backend
        shell: bash
        run: |
          set +e
          python -m pytest -q -x \
            tests/test_flowsheet_shared_topology.py \
            tests/test_process_kernel_075.py \
            tests/test_process_kernel_075_identity.py \
            tests/test_process_kernel_075_profile_identity.py \
            tests/test_process_kernel_075_runner.py \
            > "$RUNNER_TEMP/pytest-focused-output.txt" 2>&1
          status=$?
          echo "$status" > "$RUNNER_TEMP/pytest-focused-status.txt"
          exit 0
      - name: Upload focused pytest diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-focused-temp
          path: |
            ${{ runner.temp }}/pytest-focused-output.txt
            ${{ runner.temp }}/pytest-focused-status.txt
          if-no-files-found: error
          retention-days: 1
