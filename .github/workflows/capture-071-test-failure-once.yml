name: Capture 071 test failure once

on:
  push:
    branches:
      - impl/071-model-scenario-dof

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: impl/071-model-scenario-dof
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
      - name: Capture targeted output
        working-directory: backend
        shell: bash
        run: |
          set +e
          python -m pytest -q tests/test_model_scenario_dof.py > ../reports-071-targeted.txt 2>&1
          status=$?
          echo "exit_status=$status" >> ../reports-071-targeted.txt
          exit 0
      - name: Commit diagnostic and remove workflow
        run: |
          rm .github/workflows/capture-071-test-failure-once.yml
          git config user.name "jarvisos-diagnostic-bot"
          git config user.email "actions@users.noreply.github.com"
          git add reports-071-targeted.txt .github/workflows/capture-071-test-failure-once.yml
          git commit -m "Capture 071 targeted test diagnostics"
          git push origin HEAD:impl/071-model-scenario-dof
