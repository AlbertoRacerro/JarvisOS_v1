{
  "task_id": "20260829-0818-112-foundation-a",
  "target_ref": "3ec5d6c0d1bf2e9ca558e08d36597b52b3005b38",
  "mode": "candidate_patch",
  "max_turns": 36,
  "context_files": [
    "docs/specs/112-readiness-2026-08-29.md",
    "docs/specs/112-full-spec-terminal-closure-2-2026-08-29.md"
  ],
  "instructions": "IMPLEMENT ONLY BOUNDED PHASE A OF READY SPEC 112 PROJECT-KNOWLEDGE-CORE-1 against exact target 3ec5d6c0d1bf2e9ca558e08d36597b52b3005b38. This is a materially narrower replacement for earlier monolithic attempts that timed out during exploration. Do not attempt the full 112 surface in this candidate. Produce code, not an architecture report.\n\nPHASE A GOAL: establish the additive server-owned coordination persistence and pure deterministic core needed by later 112 owner-integration work, plus focused tests. Do NOT yet mutate canonical Parameter/Requirement/Assumption/ModelSpec/Decision owners and do NOT implement frontend.\n\nALLOWED WRITES ONLY: backend/app/core/schema.py; new backend/app/modules/project_knowledge/**; backend/tests/** files specifically for this Phase A. Do not modify any other existing module, AGENTS.md, STATUS.md, workflows, provider/egress/budget/Hermes code, or frontend.\n\nSTART FAST: use at most TWO exploration calls before the first write. First call should batch-read only the relevant portions of backend/app/core/schema.py and one or two nearby module/service/test examples needed to match repository conventions, with modest line bounds. The second call may search/read only an exact missing symbol. Then begin writing. Do not use all exploration budget before coding.\n\nFROZEN PHASE-A CONTRACT: add only coordination/history truth, never duplicate current Project/model values. Schema must be additive/idempotent and follow existing bootstrap/migration conventions. Implement the minimum accepted 112-owned structures needed for: (1) bounded draft identity/intents with workspace + exact parent + CAS token and ordered typed operation JSON/rows; (2) immutable working revisions/change-set identity and chain state; (3) append-only project_knowledge_validation with explicit supersedes_validation_id and enough fields to bind exact working revision, criterion/rule identity+version, validated basis digest, exact source run/result identity/digest, validator identity/version, outcome; enforce/validate one current unsuperseded row per exact slot in service logic without destructive history; (4) immutable reconciliation request records with EXACT UNIQUE(workspace_id,idempotency_key), bound working revision/target identity+digest/ack-or-policy/request digest/selected-validation-set digest and deterministic same-key conflict detection; (5) requirement_applicability relationship seam owning only workspace/requirement/model_spec/model_version applicability lifecycle/revision metadata, never Requirement payload; (6) immutable reconciled snapshot manifest rows with digest/payload sufficient for later exact historical reconstruction; (7) revision-neutral scalar result admission seam only if the accepted schema does not already have an equivalent owner, UNIQUE(run_id,output_name).\n\nSERVICE CORE: new project_knowledge service/helpers may implement deterministic create/read primitives for the above, digest/canonical-JSON helpers, validation replacement/current-selection checks, request idempotency binding checks, snapshot digest verification, and strict workspace/CAS validation. Keep APIs/internal functions small and directly testable. Do NOT fake final reconciliation or canonical owner mutation. Any function that would require owner mutation must remain absent rather than pretend atomicity.\n\nFAIL-CLOSED RULES TO TEST: wrong workspace and stale draft token; reconciliation same workspace/key exact retry returns same identity but changed bound value conflicts; PASS->FAIL and FAIL->PASS replacement makes only the later row current; stale predecessor rejected; broken/cross-slot supersession rejected; multiple-current detection fails closed; snapshot digest mismatch/corruption rejected; applicability is workspace/model scoped and does not copy Requirement data; schema/bootstrap rerun idempotent. If scalar seam is added, duplicate (run_id,output_name) is rejected and no working_revision_id is part of scalar uniqueness.\n\nNON-GOALS: no routes/UI; no canonical owner writes; no dependency graph projection yet; no proposal/replacement/freshness integration yet; no final reconciliation transaction; no provider/model call; no 113+ work. Report these as intentionally deferred Phase-B work, not defects in this candidate.\n\nQUALITY: match existing SQLite helper/error conventions instead of inventing a framework. Keep JSON bounded and validate types/lengths where repository conventions make this practical. No secret-bearing/unbounded opaque logging.\n\nCHECKS: inspect candidate diff, run git_diff_check and python_compile_changed. Submit a concise report naming exact files changed and tests added."
}
