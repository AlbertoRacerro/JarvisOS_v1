diff --git a/backend/app/modules/ai/egress_lifecycle.py b/backend/app/modules/ai/egress_lifecycle.py
--- a/backend/app/modules/ai/egress_lifecycle.py
+++ b/backend/app/modules/ai/egress_lifecycle.py
@@ -193,6 +193,7 @@ def consume_confirmation_ticket(
                 connection,
                 material=material,
                 projection=projection,
+                allow_continuation_segments=authority_json is not None,
             )
         except (EgressContractError, persistence.EgressStateError, ValueError):
             _transition_ticket(
diff --git a/backend/app/modules/ai/egress_revalidation.py b/backend/app/modules/ai/egress_revalidation.py
--- a/backend/app/modules/ai/egress_revalidation.py
+++ b/backend/app/modules/ai/egress_revalidation.py
@@ -18,4 +18,5 @@ def validate_ticket_authority_state(
     *,
     material: EgressPacketMaterial,
     projection: EgressPacketProjection,
+    allow_continuation_segments: bool = False,
 ) -> None:
@@ -33,4 +34,8 @@ def validate_ticket_authority_state(
         projection=projection,
     )
     _validate_context_authority(connection, material=material)
-    _validate_source_digests(connection, material=material)
+    _validate_source_digests(
+        connection,
+        material=material,
+        allow_continuation_segments=allow_continuation_segments,
+    )
@@ -261,4 +266,5 @@ def _validate_source_digests(
     connection: sqlite3.Connection,
     *,
     material: EgressPacketMaterial,
+    allow_continuation_segments: bool,
 ) -> None:
@@ -270,6 +276,12 @@ def _validate_source_digests(
             "ticket source digests have no workspace binding"
         )
     for source_ref, expected_digest in sorted(expected.items()):
+        if source_ref.startswith("segment:"):
+            if allow_continuation_segments:
+                continue
+            raise sensitivity.SensitivityPolicyError(
+                "ordinary confirmation ticket cannot reference protected segments"
+            )
         snapshot, _label = _resolve_current_source(
             connection,
             workspace_id=material.workspace_id,
diff --git a/backend/app/modules/ai/token_flow_confirmation_resume.py b/backend/app/modules/ai/token_flow_confirmation_resume.py
--- a/backend/app/modules/ai/token_flow_confirmation_resume.py
+++ b/backend/app/modules/ai/token_flow_confirmation_resume.py
@@ -357,7 +357,20 @@ def _validate_context(
             "continuation confirmation source digests are malformed"
         ) from exc
     segment_key = f"segment:{authority.protected_segment_index}"
-    if not isinstance(source_digests, dict) or source_digests.get(segment_key) != segment["body_digest"]:
+    segment_keys = (
+        {
+            key
+            for key in source_digests
+            if isinstance(key, str) and key.startswith("segment:")
+        }
+        if isinstance(source_digests, dict)
+        else set()
+    )
+    if (
+        not isinstance(source_digests, dict)
+        or segment_keys != {segment_key}
+        or source_digests.get(segment_key) != segment["body_digest"]
+    ):
         raise TokenFlowConflictError(
             "continuation confirmation source digest changed"
         )
