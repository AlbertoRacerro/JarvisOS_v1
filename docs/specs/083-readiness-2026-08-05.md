# Spec 083 readiness decision — 2026-08-05

**Decision:** READY for one bounded implementation PR.

**Registry transition authorized:** `planned` → `ready`

**Implementation PR:** none at readiness time.

**Exact audit baseline:**

```text
repository: AlbertoRacerro/JarvisOS_v1
branch: master
commit: 3f4ce04ba21e7e2ac4d21d9b6361e787f702bb8a
merged kernel: PR #228
merged complete definition: PR #229
```

## 1. Authority read

This audit applies, in order:

1. `AGENTS.md`;
2. `docs/AGENT_EXECUTION_AND_AUTOMATION_PROTOCOL.md`;
3. `docs/specs/STATUS.md`;
4. `docs/specs/README.md`;
5. `docs/specs/081-frontend-beta-authority-0.md`;
6. `docs/specs/070-ui-foundation-1.md` and its readiness/merged implementation evidence;
7. `docs/specs/083-app-shell-1.md`;
8. `docs/specs/083-visual-direction-decision-2026-08-05.md`;
9. `docs/specs/083-ui-foundation-checker-amendment-2026-08-05.md`;
10. `docs/specs/083-spa-fallback-amendment-2026-08-05.md`;
11. exact-master frontend and static-serving runtime.

No chat handoff, Penpot draft, screenshot, or stale branch overrides these sources.

## 2. Dependency audit

| Dependency | Required state | Exact-master state | Decision |
| --- | --- | --- | --- |
| 006 BLUECAD workbench + 3D viewer | merged | merged through implementation PR #23 | satisfied |
| 070 UI-FOUNDATION-1 | merged | merged through implementation PR #225 and reconciled | satisfied |
| 081 frontend-beta authority | definition authority only | present and binding; no implementation PR required | satisfied |

No hidden dependency on 084–092, re-derived 029/035/054/058c, or the Penpot identity is required to implement 083.

## 3. Definition completeness

The complete definition now freezes:

- exact static route table and route IDs;
- root replacement, trailing-slash normalization, not-found and development-route behavior;
- native History API implementation with no router dependency;
- exact primary and contextual navigation labels;
- exact required legacy routes and labelling;
- static four-stage registry;
- current BLUECAD mounted once through ModelStage compatibility;
- record/geometry selection seam with no current producer;
- navigator, sidecar and dock closed defaults and focus behavior;
- state and persistence restrictions;
- visual-identity exclusion and 070 reuse;
- exact responsive widths and browser evidence matrix;
- exact implementation file set;
- dependency-free shell checker requirements;
- bounded 070-checker lifecycle repair;
- bounded production SPA fallback and tests;
- acceptance, non-goals and rollback.

The four blockers recorded by the original kernel are closed:

1. global identity is separated into the Penpot lane and is no longer inferred;
2. native History API routing is selected by minimum-necessary analysis;
3. exact file set and checker contract are frozen;
4. responsive widths and route-by-route browser evidence are frozen.

## 4. Penpot separation audit

The maintainer is developing visual identity independently in Penpot.

083 is ready because it requires no unfinished Penpot value or asset. The implementation is prohibited from changing:

- global font stacks;
- semantic token values;
- palette;
- borders, radii, shadows or global surface grammar;
- iconography;
- image, texture or illustration assets;
- global motion language;
- current-page visual redesign.

083 may add only structural shell CSS using current 070 semantic roles. The later identity lane can restyle the shell without changing route, stage, selection, persistence, backend, or continuity contracts.

This makes the visual identity independently removable and prevents a hidden global redesign inside APP-SHELL-1.

## 5. Router readiness

A router package is not minimum necessary.

The route set is static and contains no dynamic parameters, nested loaders, runtime registration, server rendering, route guards, or asynchronous route modules. The selected History API boundary is implementable through:

- one typed compile-time route table;
- one `popstate` subscription with cleanup;
- native anchors with narrowly bounded internal interception;
- `pushState`/`replaceState`;
- deterministic resolution and focus movement.

The implementation adds no package, lockfile, state framework, icon system, or UI framework.

Residual risk: bespoke history/focus behavior can regress. It is controlled by exact route tests, production-serving evidence, browser back/forward proof, focus assertions, and the deterministic checker.

## 6. Production-serving readiness

Exact master currently mounts the built frontend through raw Starlette `StaticFiles(..., html=True)`, which does not satisfy arbitrary SPA direct-load fallback.

This is not an unresolved blocker because the merged amendment provides a bounded implementation contract:

- add one static adapter and focused tests;
- preserve registered API precedence;
- fallback only after ordinary static 404;
- fallback only for GET/HEAD HTML navigation requests;
- reject paths with asset suffixes;
- derive reserved backend roots from registered routes;
- keep API misses, missing assets, non-HTML requests and non-GET/HEAD methods as real errors;
- remain inactive when `frontend/dist` is absent.

The authorized backend file set is exactly:

```text
backend/app/main.py
backend/app/core/spa_static.py
backend/tests/test_spa_static.py
```

The change is delivery infrastructure only and creates no backend data or authority.

Residual risk: an overbroad fallback could mask API 404s. It is beta-blocking and must be rejected by the focused tests and FastAPI-served browser proof.

## 7. UI-foundation checker readiness

The merged 070 checker has one stale lifecycle assertion: it expects 070 `in_review/#225` although canonical status is `merged/#225`.

The implementation is authorized to repair only that assertion in `scripts/check_ui_foundation.py`. Every token, theme, primitive, color, SVG, migration, BLUECAD and reduced-motion rule remains mandatory.

Readiness requires both:

```text
python scripts/check_ui_foundation.py
python scripts/check_app_shell.py
```

to pass on the exact implementation head.

The stale assertion is therefore a bounded implementation task, not a reason to weaken or skip the 070 gate.

## 8. Current-page continuity

Every current page has one preservation path:

| Current page | 083 path |
| --- | --- |
| Dashboard | `/home` |
| BlueCAD | `/design/model` through one compatibility adapter |
| DomainFoundation | `/legacy/domain-foundation` |
| AIDraft | `/legacy/ai-draft` |
| SystemStatus | `/legacy/system-status` |
| DevLocalChat | `/legacy/dev-local-chat` in development only |

BLUECAD preservation includes real candidate actions, archive, retry/duplicate, promotion, validation, attempt history, artifact links, GLB loading, orbit, pan, zoom, resize and cleanup.

No equivalent page is deleted, cloned into a second state store, or represented by fake data.

## 9. Scope absorption audit

083 does not absorb:

- 084 candidate aggregate/read model;
- 085 full BLUECAD migration;
- 086 geometry inspection;
- 087 lineage;
- 088 Runs workbench;
- re-derived 035 Engineering Data;
- 089 analytics;
- 090 AI threads;
- 091 Jarvis behavior;
- 092 scene binding;
- re-derived 029 Settings;
- re-derived 054 proposal review;
- re-derived 058c semantic scene tools;
- editable flowsheets;
- Penpot visual identity.

Results, Review, Flowsheet, migration-pending destinations, sidecar and dock must remain honest shells or unavailable states.

## 10. Security and authority audit

The implementation requires no change to:

- FastAPI + SQLite engineering authority;
- provider routing through `run_ai_task`;
- safe defaults or fake-provider tests;
- credentials or secret storage;
- budget, spend, ledger, egress or audit;
- MemoryStore proposal/promotion boundaries;
- BLUECAD candidate lifecycle;
- artifact contracts;
- runner authority;
- Three.js internals.

No shell state except the existing 070 appearance enum may be persisted. No secrets, provider responses, prompts, engineering records, selections, panel state or routes may enter browser storage.

## 11. Accessibility and responsive readiness

The implementation contract can demonstrate:

- skip link and landmarks;
- native link semantics and `aria-current`;
- visible text navigation;
- focus after push/replace/popstate;
- panel opening focus, Escape close and toggle restoration;
- no focus in closed panels;
- system/light/dark and live system-theme continuity;
- reduced motion;
- non-color route/state distinction;
- no document-level horizontal overflow;
- local table/viewer overflow only;
- 200% zoom and compact-desktop reflow.

The exact matrix is frozen at:

```text
1440×900 at 100%
1024×768 at 100%
1440×900 at 200%
1280×720 at 100%
```

FastAPI-served direct-load/refresh evidence is required in addition to frontend build/preview evidence.

## 12. Exact implementation boundary

One implementation branch and one implementation PR are authorized after this readiness decision merges.

The implementation may modify/add only the files listed in the full spec and its two amendments. In particular:

- no current page implementation file;
- no API client;
- no theme utility;
- no primitive;
- no token values;
- no package manifest or lockfile;
- no Three.js viewer;
- no workflow;
- no other backend file.

A newly discovered required file stops implementation and requires a canonical amendment before work continues.

## 13. Deterministic and browser gates

Required exact-head gates:

```text
python scripts/check_ui_foundation.py
python scripts/check_app_shell.py
cd frontend && npm ci && npm run build
cd backend && python -m ruff check app tests
cd backend && python -m pytest -q
repository-standard BLUECAD geometry canary/property/time proof
CI terminal success
```

Required browser/operator evidence includes:

- all production routes direct-load and refresh through FastAPI-served `frontend/dist`;
- root replacement, trailing slash, back/forward and not-found behavior;
- production rejection of the dev route;
- missing asset and reserved API-root misses remain server 404s;
- all panels and focus behavior;
- exact responsive matrix and overflow assertion;
- current pages and real BLUECAD continuity;
- handled backend-unavailable state;
- no live provider call and no secret-bearing screenshot.

## 14. Rollback audit

Rollback can:

1. restore baseline App component-state switching;
2. restore baseline Layout sidebar/button navigation;
3. remove all new shell, stage, routing and checker files;
4. remove shell CSS import/responsive rules;
5. restore the raw static mount;
6. retain all pages, API contracts, 070 foundation, BLUECAD, database and artifacts;
7. require no migration or browser-storage cleanup.

The implementation is independently removable and leaves no durable state.

## 15. Finding disposition

| Severity | Finding | Disposition |
| --- | --- | --- |
| P0 | none | closed |
| P1 | stale 070 checker lifecycle assertion | bounded repair authorized; exact gate retained |
| P1 | missing production SPA fallback | bounded adapter/tests authorized; API isolation mandatory |
| P2 | none current | closed |
| P3 | temporary current aesthetic | outside 083; Penpot lane |

No current blocker remains at readiness time.

## 16. Readiness decision

Spec 083 is **READY** for one bounded implementation.

The registry may record `ready` only after this readiness PR merges. The implementation must then:

1. create one branch from the verified post-readiness master;
2. set row 083 to `in_progress` on that branch;
3. implement only the exact full-spec boundary;
4. open one implementation PR and set row 083 to `in_review` with that PR number;
5. run all exact-head deterministic and browser gates;
6. close all P0/P1 and beta-blocking P2 findings;
7. merge with `expected_head_sha` and no auto-merge;
8. reconcile row 083 to `merged` immediately after merge.
