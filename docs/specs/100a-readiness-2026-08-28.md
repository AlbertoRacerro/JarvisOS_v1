# 100a — CODEBASE-LEAN-AUDIT-1 readiness decision

Date: 2026-08-28  
Verdict: **READY**  
Exact readiness base: `e05c214bc601c8741e479f33aaee09b6485a60b4`

## Authority checked

- `AGENTS.md` at the exact readiness base.
- `docs/AGENT_EXECUTION_AND_AUTOMATION_PROTOCOL.md` at the exact readiness base.
- `docs/specs/STATUS.md` at the exact readiness base.
- `docs/specs/README.md` at the exact readiness base.
- `docs/specs/100a-codebase-lean-audit-1.md` at the exact readiness base.
- Open pull requests at derivation time: none.
- `100` and the later frontend-fusion prerequisite `100f` are both merged on the readiness base.

The maintainer release of 2026-08-28 lifts the post-100f inspection hold only far enough to complete 100a and then derive the bounded 100b disposition. It does not authorize 100c, 101–110, backend feature work, frontend feature work, or runtime cleanup under 100a.

## Readiness decision

100a is ready as an **audit-only** implementation slice. Its implementation may inspect and measure the complete first-party codebase and may commit the required audit artifact under `docs/audits/`. It may not delete, rewire, rename, inline, merge, refactor, migrate, or otherwise change product/runtime behavior.

The implementation must audit the exact implementation base and record its full SHA. If docs-only lifecycle commits advance `master` after this readiness decision, the audit records both the readiness base above and the exact audited implementation-base SHA; conclusions about code reachability and semantic surface are bound to the latter.

## Frozen execution boundary

The implementation must cover every first-party execution area required by the full spec, including:

- `backend/app/**`;
- `frontend/src/**`;
- `backend/tests/**` and frontend tests as behavior/ownership evidence;
- `scripts/**`;
- `.github/workflows/**`;
- first-party launchers and execution-relevant configuration.

Generated, vendored and persisted runtime data remain excluded except when they expose an active ownership problem.

Every audited component receives exactly one primary disposition from the vocabulary in the full spec, with evidence, confidence, and owner/capability mapping. `Unused` is never sufficient evidence for `DELETE`. Desired-but-unwired capabilities remain `WIRE`/`DEFER`; ambiguous intent remains `UNKNOWN`.

Every `DELETE` recommendation must satisfy all seven deletion-gate conditions explicitly. A failed or uncertain condition forbids `DELETE`.

## Required evidence

The audit artifact must include:

1. the exact audited SHA;
2. reproducible tracked first-party LOC and file/module counts by major area/language;
3. backend route ownership/count, frontend API/client surface count, workflow/script count, and useful dependency/complexity/duplication indicators;
4. full first-party coverage and a component disposition inventory;
5. a separate desired-but-unwired capability register;
6. a ranked removal/simplification ROI-and-risk register;
7. explicit deletion-gate evidence for every `DELETE` recommendation;
8. measured-versus-unmeasured performance findings, using `PROFILE` where measurement is absent;
9. findings routed to later existing authorities such as 101, 103, 104 and 105 rather than pre-empted here;
10. one bounded candidate set for fresh 100b derivation.

Temporary/local standard-library analysis scripts are preferred. Permanent tooling is out of scope unless the implementation proves a narrow repeated need and documents why it is minimum necessary.

## Safety and cost constraints

- No live provider calls, provider spend, new credentials, new accounts, or new dependencies.
- No schema/API semantic change.
- No product feature wiring.
- No frontend visual/IA change.
- No process-upstream selection or strangling that belongs to 103/104.
- No engineering-domain cleanup that belongs to 105.
- No canonical-write remediation that belongs to 101.

## Deterministic gate

Before merge, the exact implementation head must satisfy the repository-required deterministic gates applicable to the docs/audit-only diff and any spec-status checks. No gate or review from an older head may authorize merge.

## Result

`100a CODEBASE-LEAN-AUDIT-1` may move from `planned` to `ready`. The next runtime-authorized action after this readiness/registry decision is the normal 100a implementation lifecycle, beginning from the exact then-current `master` and remaining strictly audit-only.
