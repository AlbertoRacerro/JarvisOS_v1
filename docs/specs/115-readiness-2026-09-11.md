# 115 PROJECT-SEARCH-1 readiness — 2026-09-11

Status: **READY** for bounded implementation once this readiness PR is merged and canonical `docs/specs/STATUS.md` records 115 as `ready`.

Exact readiness base: `ef6ea7b1e5d7a3b9aec4a478d5856382ff992dc3`.

Accepted implementation contract: `docs/specs/115-project-search-1.md` as merged through PR #611.

## Gate verification

Fresh exact `master` confirms every readiness gate in the accepted contract:

- dependencies 112, 113 and 114 are all `merged` in the canonical registry;
- no active 115 implementation or readiness PR existed when this branch was created;
- Project Basis/modeling already exposes bounded workspace-scoped literal selection through the existing 042 `select_context_records` path and its FTS5/fallback behavior;
- Model Dossier still owns bounded exact workspace/model/version reads in `backend/app/modules/modeling/model_dossier.py`;
- Literature still owns bounded workspace-scoped source/entry/provenance reads in `backend/app/modules/memory/literature_service.py`;
- the canonical frontend insertion point remains the Memory / Project Basis composition in `frontend/src/App.tsx` at `/memory/project-basis`;
- the generic trusted browser-proof controller and declarative-plan directory remain present on `master`, including the 113/114 compatibility cases needed to prove cross-owner Memory behavior;
- no new persistent search table, migration, background index or second project/model/literature owner is required for the accepted V0.

The minimum-necessary conclusion is therefore to reuse owner-local reads plus the existing FTS/fallback capability; introducing another index/store would fail readiness rather than improve it.

## Implementation boundary frozen by readiness

Implementation is authorized only as the read projection defined by `115-project-search-1.md`: a dedicated project-search aggregator over owner-local bounded read helpers, deterministic literal exact/prefix/contains ordering, exact identity/provenance projection, canonical owner-route navigation, and context-neutral browsing.

Owner-read failure remains fail-whole-request: 115 must not silently omit a failing domain and present partial results as a complete search response.

The implementation must return to planning instead of widening scope if correctness or bounded latency is found to require a new persistent search/index store, schema migration, semantic/vector retrieval, provider/network egress, arbitrary filesystem access, Jarvis context mutation, or a second canonical owner.

## Registry transition

This readiness PR owns the single canonical lifecycle transition `115: planned → ready`; it does not start product implementation. Any implementation branch must re-read the merged `master` registry and observe `ready` before starting.

## Evidence required before merge

The implementation candidate must satisfy the deterministic backend/frontend acceptance in the accepted spec and a trusted exact-head real-Chromium proof on `/memory/project-basis`. The proof must remain declarative over the generic post-143 executor; no `prove115` control branch or candidate-authored privileged execution is authorized.

No independent external-model review is required merely for this docs-only readiness transition. Implementation review remains proportional to the actual final diff and any authority-sensitive changes discovered there.
