# 138 PYTHON-RUNNER-HASHSEED-DETERMINISM-1 — readiness decision

Decision: READY

Exact-master basis: `f20eacb635e7da6d058a9c955edd6056aaf75eb7`

## Fresh evidence

The production runner still creates its own isolated child environment in `backend/app/modules/runner/local_python.py::execute_python_script` but does not set `PYTHONHASHSEED`. The existing runner tests exercise the real subprocess path and already inspect the allowlisted environment metadata. No new dependency, store, API, provider, egress owner, or architecture surface is needed.

The earlier spec 130 proof is not implementation authority for this repair: its hash-seed pin lived in a separate BLUECAD test helper and did not change the production runner. This readiness decision therefore authorizes only the bounded F6 residual-risk repair defined by spec 138.

## Readiness checks

- Scope is closed to one production runner environment entry and focused regression proof.
- Existing `PYTHONIOENCODING` and conditional `PYTHONDONTWRITEBYTECODE` semantics are preserved.
- Parent-environment inheritance remains forbidden.
- The causal proof must spawn two independent children through `execute_python_script` and demonstrate stable hash-dependent output with child-visible `PYTHONHASHSEED=0`.
- No generic determinism framework, RNG framework, sandbox redesign, runner recovery, artifact-model work, BLUECAD work, or calc-runner work is authorized.
- Required implementation paths are limited to `backend/app/modules/runner/local_python.py`, `backend/tests/test_python_runner.py`, and lifecycle registry bookkeeping.
- Material implementation requires normal exact-head CI and independent semantic review before merge.

## Decision

READY once the canonical registry row for 138 is `ready`. The implementation should be the smallest causal patch described above; any broader environment propagation or determinism redesign requires fresh authority.
