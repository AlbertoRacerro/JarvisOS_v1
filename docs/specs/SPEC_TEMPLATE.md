# NNN — <Title>

Status: draft
Depends on: <spec numbers or "none">

## Goal

One paragraph: what exists after this slice that did not exist before.

## Why

2–4 lines linking the slice to strategy. Enough for an agent to make small in-scope
judgment calls correctly.

## Scope

In scope:
- ...

Out of scope (binding non-goals):
- ...

## Files likely touched

Verify against actual code before starting; report conflicts instead of guessing.

- `path/one`
- `path/two`

## Design constraints

- Hard requirements the implementation must respect (schema shapes, invariants,
  patterns to follow, things that must NOT change).

## Acceptance criteria

Checkable statements. Each one must be objectively verifiable from the diff or a test.

1. ...
2. ...

## Required tests

- Concrete tests to add, offline-only, using fake providers/fixtures.

## Definition of done

Test gate green (see `AGENTS.md`), acceptance criteria met, spec status updated,
summary written.
