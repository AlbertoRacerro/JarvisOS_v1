# 085 BLUECAD-WORKBENCH-2 — readiness decision (2026-08-13)

## Decision

Spec 085 is implementation-ready **only after this readiness PR merges together with registry promotion of row 085 to `ready`**. Until then, runtime implementation remains unauthorized.

Derivation baseline: `master` at `64f680adc290ba2c50d6d9dba80baa3ae7669a0d` after merge of planning PR #237.

Dependencies verified merged: 006, 006c, 083, 084.

## Re-audit of current runtime

The current runtime still compatibility-mounts `pages/BlueCAD.tsx` from `ModelStage.tsx`. The legacy page owns workspace/candidate discovery, selection, create, archive, duplicate-brief, promotion, validation-report loading, attempt history and real GLB embedding. The 084 aggregate client already exists and is not yet consumed by the page.

`PrimaryStageProps` already exposes the accepted `StageSelection` seam and includes `bluecad-candidate` as a typed `RecordRef` resource. No URL-owned candidate subject seam is required for 085; candidate identity therefore remains stage-local while the selected candidate is published through `onSelectionChange`.

The 083 shell exposes navigator, sidecar and dock containers, but today those containers own fixed generic content and have no stage-content injection seam. 085 may add one minimal presentation-only shell-region contribution contract so Model stage can populate these already-existing regions. This is not a new engineering state store and must not persist server data outside the BLUECAD workbench owner.

## Frozen composition decision

085 uses one BLUECAD workbench owner component under Model stage. That owner is the only place that owns BLUECAD discovery/detail request state, mutation state, archived visibility, local candidate filter text, validation-report detail and request-generation guards.

The owner supplies presentation nodes to the existing shell regions through a minimal additive shell contribution seam:

- contextual navigator: workspace selector, explicit manual Refresh, archived/filter controls and candidate list;
- primary stage: compact candidate/action strip plus dominant real GLB viewport;
- contextual sidecar: lifecycle, freshness, validation/evidence/artifact/diagnostic detail;
- analysis dock: ordered attempts and explicit aggregate-linked evidence/run references.

Shell containers remain responsible for open/close/focus/Escape behavior. BLUECAD region children must not duplicate those responsibilities.

No second router, global state library, backend endpoint, schema, cache or scene identity is authorized.

## Request and selection rules

1. Workspace list and candidate list remain discovery APIs.
2. Candidate selection loads exactly one 084 aggregate for visible detail.
3. Every workspace/candidate detail request carries a monotonically increasing request generation or equivalent abort guard; stale responses cannot update visible state.
4. Candidate selection publishes `{ kind: "record", ref: { resource: "bluecad-candidate", workspaceId, recordId } }`.
5. Workspace change, archive, archived-filter change or missing aggregate must revalidate selection deterministically.
6. Mutation success is claimed only after backend confirmation and canonical reload.
7. Create selects the returned candidate after reload; duplicate-brief only copies brief text into the creation form; archive hides/reselects deterministically when archived rows are hidden; promote remains limited to canonical valid/unpromoted candidates.
8. A response belonging to a previous workspace/selection must never mutate the current workbench.
9. Manual Refresh remains an explicit navigator action because polling/websockets are out of scope. It reloads canonical workspace/candidate discovery and the selected candidate detail using the same request-generation guards, preserving selection when the candidate still exists and deterministically revalidating it otherwise.
10. Create/archive/promote mutations must capture the workspace/selection context and a mutation generation when started. If that context is no longer current when the mutation completes, its completion may not overwrite current selection, messages, list/detail state or visible mutation outcome. The backend result remains authoritative and may be observed only through a canonical reload in the current context. The production state helper and executable harness must prove stale mutation-completion rejection for at least create and one candidate-targeted mutation (archive or promote), including a workspace switch while the request is in flight.

## Data authority rules

Visible detail consumes the 084 aggregate for candidate, artifacts, evidence, runs, freshness and diagnostics. TypeScript must not reconstruct evidence, provenance, run association or freshness joins.

Detailed validation checks may still come from the canonical validation-report artifact because 084 intentionally does not reinterpret report JSON as evidence. Missing or malformed report content is a local validation-detail failure only and must not hide otherwise valid candidate/GLB/aggregate data.

The GLB viewer continues to receive only the existing secure backend artifact-content URL. `stored_path`, direct data-root paths and frontend filesystem access are forbidden.

Lifecycle, freshness, validation and promotion are distinct concepts and must be rendered separately.

## Viewer decision

`BluecadGlbViewer` remains the real renderer. 085 must add bounded resource cleanup for native embedding and candidate/artifact churn: on artifact change or unmount, every loaded GLTF scene resource owned by the viewer must be traversed and its geometries, materials and textures disposed; renderer/controls cleanup remains required. If a GLTF load completes after the request is stale or the component has unmounted, the late-arriving scene must be disposed immediately rather than merely ignored. The cleanup must not dispose shared external resources the viewer does not own. A deterministic regression check plus browser/manual evidence must cover artifact replacement and late-load disposal. No measurement, isolate/hide, clipping, persistent object identity, semantic hit mapping or binding manifest is authorized; those belong to 086/092.

## Exact implementation allow-list

Runtime implementation is limited to this set unless a formal amendment is merged first:

```text
frontend/src/App.tsx
frontend/src/stages/registry.ts
frontend/src/stages/ModelStage.tsx
frontend/src/pages/BlueCAD.tsx
frontend/src/components/Layout.tsx
frontend/src/components/BluecadGlbViewer.tsx
frontend/src/components/shell/ContextualNavigator.tsx
frontend/src/components/shell/ContextualSidecar.tsx
frontend/src/components/shell/AnalysisDock.tsx
frontend/src/components/bluecad/BluecadWorkbench.tsx
frontend/src/components/bluecad/BluecadNavigator.tsx
frontend/src/components/bluecad/BluecadSidecar.tsx
frontend/src/components/bluecad/BluecadAnalysisDock.tsx
frontend/src/components/bluecad/workbenchState.ts
frontend/src/components/bluecad/workbenchStateHarness.ts
frontend/src/api/client.ts
frontend/src/styles/global.css
scripts/check_app_shell.py
scripts/check_bluecad_read_model.py
scripts/check_bluecad_workbench.py
docs/specs/STATUS.md
```

New BLUECAD component files may be omitted if the implementation is smaller without them. The two state/harness files are required only if the implementation uses the frozen pure-state harness design below; an amendment is required before substituting a materially different runnable test surface. No other path is authorized by default.

`frontend/src/styles/global.css` is authorized only for bounded BLUECAD/workbench-local selectors needed by 085. It may not be used to change global element rules, 070 token values, shared component grammar, global font, palette, iconography, radii, shadows or motion.

The two inherited checker files are authorized **only** for the preservation reconciliations below. They are not general 085 implementation surfaces and may not weaken their merged 083/084 semantic checks.

Explicitly forbidden: backend files, migrations/schema, package manifests, lockfiles, workflows, provider/credential/budget/egress code, Penpot assets, global font/palette/token-value replacement, new dependencies.

## Required inherited-checker preservation reconciliations

The merged 083 and 084 checkers remain mandatory gates, but their implementation-time scope guards encode assumptions that were correct only while those slices were active. A conforming 085 branch necessarily changes later-slice files and consumes the 084 aggregate, so readiness authorizes exactly these bounded lifecycle reconciliations before the 085 implementation PR opens.

### `scripts/check_app_shell.py`

When the canonical Registry row for 083 is `merged` with exact implementation PR #231, the checker must operate in **post-merge preservation mode on later branches** rather than requiring the complete repository diff from the historical 083 implementation base to equal the original 27-file active footprint.

Preservation mode must still fail if the canonical 083 row/PR is wrong or ambiguous, a required 083 shell contract/file is missing, or a current checked shell invariant regresses. It may permit unrelated later-slice paths because the active slice checker owns current-PR scope. The historical strict active-083 path-set behavior may remain only for a genuine 083 `in_review/#231` context; it must not be selected merely because a later checkout is not canonical `master`.

Self-tests must prove at minimum:

- canonical `merged/#231` plus unrelated later-slice A/M paths is accepted;
- canonical `merged/#231` still rejects a missing/tampered required 083 contract;
- canonical `in_review/#231` retains the historical exact active-scope behavior if that compatibility path is kept;
- wrong PR, ambiguous/decoy registry evidence and invalid lifecycle evidence still fail closed.

### `scripts/check_bluecad_read_model.py`

When the canonical Registry row for 084 is `merged` with exact implementation PR #236, the checker must operate in **post-merge preservation mode**. In that mode it must continue checking the 084 aggregate response models, query-only snapshot, route binding, typed client and forbidden `stored_path` contract, but it must not reject later-slice changed paths and must not reject UI consumption of `getBluecadCandidateAggregate`.

The original 084 exact allow-list and `check_no_ui_consumption()` rule remain historical active-084 constraints only. They must still be covered by self-tests or an equivalent explicit active mode, but they cannot be applied to 085 where aggregate consumption is mandatory.

Self-tests must prove at minimum:

- canonical `merged/#236` accepts a later BLUECAD UI consumer and unrelated later-slice A/M paths;
- merged preservation mode still rejects a broken 084 aggregate contract, missing typed client/route binding or exposed `stored_path`;
- an active historical 084 mode, if retained, still rejects out-of-scope files and UI consumption;
- wrong/ambiguous registry evidence fails closed.

No runtime/backend authority, response shape or aggregate semantics may change as part of either checker reconciliation.

## Executable state-transition harness

The request-race, selection-revalidation and mutation-conflict rules above must be executable, not source-text-only assertions.

085 therefore freezes one dependency-free pure TypeScript state-transition helper at `frontend/src/components/bluecad/workbenchState.ts`. `BluecadWorkbench` must delegate request-generation acceptance, selection revalidation, stale mutation-completion rejection and mutation-conflict decisions to this helper rather than reimplementing equivalent logic inside React effects or event handlers.

`frontend/src/components/bluecad/workbenchStateHarness.ts` is a non-React executable harness over that exact helper. It may use only Node/TypeScript standard capabilities already present after `npm ci`; it must not require a new package or browser. The harness must exit non-zero on any failed assertion and must exercise at minimum:

- workspace A list/detail response arriving after workspace B is current and being rejected;
- candidate A detail response arriving after candidate B becomes selected and being rejected;
- current-generation response acceptance;
- manual Refresh preserving selection when the candidate still exists and deterministic reselection/clear when it does not;
- archived-filter hiding the selected candidate and deterministic revalidation;
- archive/promote mutual exclusion while a conflicting mutation is pending;
- create mutation completion after a workspace switch being rejected from current visible state;
- archive or promote completion after workspace/selection change being rejected from current visible state;
- duplicate-brief remaining a creation-form transition rather than a backend mutation or silent candidate clone.

The harness is compiled with the repository's locked TypeScript compiler and executed with Node. It is not allowed to duplicate an independent implementation of the state rules: it imports the production helper. `scripts/check_bluecad_workbench.py` must verify that the workbench imports/uses that helper and that the harness imports the same module.

This harness covers deterministic state/race semantics. Rendering, focus, GLB and responsive behavior remain covered by build/checker plus the browser/manual acceptance evidence below; source checks alone are not accepted as proof of asynchronous state correctness.

## Checker and deterministic proof

Add dependency-free `scripts/check_bluecad_workbench.py` with a self-test. It must fail if:

- `ModelStage` still compatibility-mounts the old stacked page as the primary composition;
- visible workbench code does not consume `getBluecadCandidateAggregate`;
- client-side freshness/evidence/run authority is reconstructed;
- direct filesystem/data-root/provider access appears in 085 UI code;
- mock workstation values or fake AI/FEM/health/confidence telemetry are introduced;
- forbidden files or dependency/workflow/schema/global-identity surfaces change;
- candidate selection does not use the typed BLUECAD `RecordRef` seam;
- the explicit manual Refresh control from 006c is removed without a formally authorized replacement;
- the production workbench does not import/use `workbenchState.ts` for the frozen request, selection, stale-mutation and mutation-conflict decisions;
- the executable harness does not import the same production state helper;
- `BluecadGlbViewer` does not implement owned GLTF scene disposal on replacement/unmount and late stale-load disposal;
- either inherited checker reconciliation broadens beyond the exact preservation behavior frozen above.

Deterministic test/harness coverage must include at minimum:

- workspace change with an older list/detail response arriving late;
- candidate A detail response arriving after candidate B becomes selected;
- manual Refresh after remote candidate/lifecycle change, including selection preservation or deterministic reselection;
- archived-filter hiding the selected candidate;
- archive and promote pending-state conflict prevention;
- stale create completion after workspace switch;
- stale archive/promote completion after context change;
- duplicate-brief semantics;
- aggregate 404/partial diagnostics;
- no GLB, GLB load failure, artifact replacement cleanup and late stale-load disposal;
- no validation report and malformed validation report;
- zero attempts;
- well-formed attempt error detail and malformed `attempt.error_detail_json` preserved as inspectable diagnostics rather than dropped or crashing the dock;
- lifecycle/freshness/validation/promotion displayed independently.

The first eight state-transition cases above must be proven by the executable TypeScript harness. Aggregate/GLB/validation/attempt-detail/empty-attempt presentation cases may be proven through deterministic rendering/source contracts plus browser/manual evidence, but may not be silently omitted.

## Accessibility and layout proof

Implementation evidence must prove:

- candidate rows are keyboard reachable and expose selected state;
- critical actions are reachable without pointer-only interaction;
- busy/disabled mutation state is accessible;
- shell navigator/sidecar/dock retain focus-return and Escape behavior;
- GLB canvas has accessible labeling plus adjacent textual state;
- viewer does not trap focus;
- state meaning is not color-only;
- reduced motion remains respected;
- compact desktop and 200% zoom have no page-level horizontal overflow and leave a usable primary viewport.

## Visual boundary

The maintainer-approved workstation reference is binding for local 085 information hierarchy: dominant viewport, slim navigator, compact command/status chrome, right contextual detail, lower engineering dock, light/off-white technical surfaces, restrained depth and selective natural leaf/chlorophyll green accents.

085 must express that direction using existing 070 semantic contracts and bounded BLUECAD-local styling. It must not canonize global font, palette, iconography, shared-component grammar, radii, shadows or motion. Those remain the independently removable visual-identity lane.

No fake Sketch/Extrude/Fillet tools, AI confidence, health percentages, stress values, FEM jobs, analytics or Jarvis chat may be copied from the reference.

## Exact-head gates

Before opening the implementation PR, first apply and self-test the two bounded inherited-checker preservation reconciliations above, then run:

```bash
python scripts/check_spec_status.py --self-test
python scripts/check_ui_foundation.py
python scripts/check_app_shell.py
python scripts/check_bluecad_read_model.py
python scripts/check_bluecad_workbench.py --self-test
python scripts/check_bluecad_workbench.py
cd frontend
npm ci
./node_modules/.bin/tsc src/components/bluecad/workbenchState.ts src/components/bluecad/workbenchStateHarness.ts --target ES2022 --module commonjs --moduleResolution node --skipLibCheck --outDir /tmp/jarvisos-bluecad-workbench-harness
node /tmp/jarvisos-bluecad-workbench-harness/workbenchStateHarness.js
npm run build
```

The harness command must run after the locked `npm ci`, emit CommonJS because its generated output executes outside the frontend package scope under `/tmp`, write generated output only under `/tmp`, and leave the repository worktree unchanged.

On the implementation PR, the same deterministic gates plus repository CI and BLUECAD Real Tool Proof must be green on the exact PR head. Any head mutation invalidates prior gate evidence.

Merge requires: exact-head diff within the allow-list; no current P0/P1 or beta-blocking P2; working lifecycle parity; real GLB preserved without owned-resource leaks across candidate/artifact churn; no fake data; no global overflow; expected-head merge guard; immediate registry reconciliation.

## Rollback

085 must remain independently removable. Reverting its implementation merge must restore the 083 compatibility-mounted BLUECAD path without reverting 083 or 084 and without changing backend engineering truth.

If the minimal shell contribution seam proves insufficient without a broader shell redesign, stop before runtime delivery and invoke the controlled 081 re-derivation path rather than hiding a global redesign inside 085.
