# 116 ROADMAP-CALENDAR-1 readiness decision — 2026-09-12

Status: readiness evidence for `116 ROADMAP-CALENDAR-1`.
Exact audit base: `81f44cb6f011fe92415c71420c6e61d7f29507b2`.
Accepted contract: `docs/specs/116-roadmap-calendar-1.md`.

## Decision

The remaining readiness questions are resolved on the exact audit base. After this readiness diff is accepted, the canonical registry may move `116` from `planned` to `ready`; product implementation must not start before that explicit registry transition.

## Persistence and migration owner

No existing mutable owner stores Roadmap work items or Calendar allocations. The existing `events` table/module remains audit evidence only and must not become the primary state store.

Implementation is therefore bounded to one additive Development-domain persistence owner:

- add `backend/app/core/development_schema.py` for module-owned schema constants/statements;
- register those statements and migration evidence through the existing `backend/app/core/database.py` initialization path, following the current modular schema pattern used by Literature;
- use the next additive migration identity after current `0018_literature_knowledge`: `0019_roadmap_calendar`;
- create dedicated normalized tables for Roadmap items, Roadmap dependency edges, typed Roadmap object links, and Calendar allocations rather than encoding mutable state in event payloads or a generic entity/task store;
- preserve workspace foreign keys, stable IDs, immutable creation provenance, mutation timestamps and exact optimistic-concurrency tokens (`updated_at` or an equivalent server-owned revision token);
- add only indexes/constraints required for workspace-scoped identity, dependency/link integrity and bounded chronological reads.

No Alembic/framework migration, second database, cache, event-sourced owner, Board store, generic workflow/task engine, recurrence engine or background scheduler is authorized.

## Backend/API owner

Use a single additive module boundary under `backend/app/modules/development/` with typed request/response models, service/repository logic and one router registered in `backend/app/main.py`.

That owner is limited to workspace-scoped Roadmap/Calendar CRUD plus deterministic Roadmap dependency and lifecycle transition validation. It must reuse the shared workspace/domain-error translation and existing redacting event logger for accepted mutation evidence. Unknown request fields, cross-workspace refs, stale CAS tokens, dependency self/cycles, unresolved dependent deletion and ambiguous/nonexistent local time must fail closed before mutation.

The Calendar persistence contract stores unambiguous instants plus the supplied valid IANA time-zone identity. Ambiguous DST folds require an explicit offset/fold resolution; gaps are rejected. Roadmap project windows and Calendar allocations remain distinct records and no edit/delete on either side silently rewrites the other.

## Frontend insertion points

Fresh exact-master routes already reserve the accepted composition:

- `frontend/src/app/routes.ts` owns `/development/roadmap/timeline` and `/development/roadmap/calendar` under the existing `ROADMAP_STAGE_ITEMS` peer navigation;
- `frontend/src/App.tsx` is the canonical route-composition insertion point;
- the AI route registry already recognizes the two Development routes, but 116 adds no Jarvis COMMIT/PROPOSE authority.

Implementation may add bounded Development API/client/components beneath those routes, but canonical Roadmap/Calendar state remains server-owned. There is no standalone Board route/store.

## Deterministic verification

Implementation acceptance must include focused backend tests for:

- restart persistence and workspace isolation;
- stale CAS refusal without partial mutation;
- self/cyclic/cross-workspace dependencies and typed links;
- blocked `Done` when `done_when` is unsatisfied or not evaluable;
- deletion refusal while dependents or linked Calendar allocations remain;
- deterministic IANA-zone round-trip plus DST fold/gap cases;
- zero silent mutation between Roadmap windows and Calendar allocations;
- event logging as evidence only, never as read authority.

Frontend deterministic coverage must prove Timeline and Calendar consume server-owned state, preserve one Roadmap identity across zero/one/many Calendar links, expose no Board state, and keep unavailable Jarvis proposal actions inert.

## Trusted browser proof

The generic exact-head browser-proof toolbox is already available and must be reused rather than extended with spec-coded controller logic. Add task intent as declarative compatibility data:

- `.github/browser-proof/plans/116-roadmap-calendar.json` using the existing validated `jarvisos.browser-proof-plan.v1` vocabulary;
- a bounded 116 fixture only if canonical test data cannot be established through existing generic fixture capabilities;
- no arbitrary model-authored JavaScript/shell/Python and no new privileged primitive unless a concrete acceptance step is otherwise impossible and separately justified.

The exact-head Chromium journey must cover the accepted contract: create one Roadmap item, edit it, create two linked Calendar allocations, verify the Roadmap window remains distinct from occupied Calendar blocks, demonstrate a blocked `Done` transition, and exercise edit/delete behavior without hidden cross-owner mutation.

## Duplicate/authority check

At readiness audit time there is no open implementation or readiness PR for 116. Dependency `111` is merged. No provider, Git, filesystem, runner, external-network, credential or destructive authority is required by this slice.

## Readiness conclusion

The persistence/API owner, additive migration shape, existing frontend insertion points, deterministic verification and generic exact-head browser-proof path are now frozen tightly enough for implementation. The next canonical action is the mechanical registry transition `116: planned -> ready`; implementation remains unauthorized until that transition is merged.
