# 113 MODEL-DOSSIER-1 — readiness decision — 2026-08-30

Decision: **READY after this planning packet is merged and `docs/specs/STATUS.md` is reconciled from `planned` to `ready` on fresh exact master.**

This readiness decision is derived from exact planning base `c590afddeec39393074600a91ddf23d14abdfbfd` and the full implementation contract in `docs/specs/113-model-dossier-1.md`. It is part of the post-112 low-risk planning-compression path; it does not itself authorize implementation while the live registry remains `planned`.

## Authority and dependency check

Hard dependencies on the planning base are satisfied:

- 050 FLOWSHEET-1 — merged;
- 051 FLOWSHEET-RECALC — merged;
- 077 EVIDENCE-EGRESS-0 — merged;
- 112 PROJECT-KNOWLEDGE-CORE-1 — merged through implementation PR #432 and reconciled on master.

The post-112 controlled-parallel profile is active. No implementation PR is open at this decision point.

## Exact-master ownership findings

The implementation can remain additive/read-only without a new durable owner:

- `model_specs` and `model_versions` already own model/version identity;
- `simulation_runs` already owns exact run history and 112 revision binding;
- `run_artifacts`/`artifacts` already own artifact linkage/metadata;
- 050/051 already own dependency/freshness semantics;
- 044/077 already own evidence provenance/classification;
- the existing modeling service/router can host or delegate one bounded read projection;
- `/memory/models` already exists as the accepted operator presentation target.

No schema or migration is required by the accepted boundary. If implementation proves that a schema/migration is necessary, this readiness decision is invalid and 113 returns to planning before any such mutation.

## Frozen implementation boundary

Allowed behavior:

- one server-owned, workspace-isolated, exact-`model_version_id` read projection;
- bounded deterministic model/version index and dossier detail;
- structured exposure of already persisted ModelSpec/version/run/artifact/evidence/freshness/revision metadata where exact linkage is proven;
- explicit unknown/empty/unavailable states otherwise;
- frontend binding of `/memory/models` to those truthful reads;
- local presentation/selection state only.

Forbidden behavior:

- any canonical ModelSpec/model-version/assumption/parameter/requirement write;
- new store/cache/table/migration;
- inferred `latest`, `current`, `healthy`, `pass`, `aligned`, `best`, or equivalent model-quality state;
- solver/rerun/recompute/validation authority;
- new freshness/evidence semantics;
- direct filesystem/provider/GitHub/frontend execution;
- 114/115/121 scope;
- opportunistic closure/refactor or new consumers of legacy direct-write modeling endpoints.

## Failure-mode acceptance matrix

Implementation is not accepted unless exact-head tests prove:

1. cross-workspace exact-version probes fail closed;
2. ModelSpec-with-no-version and version-with-no-runs remain truthful empty states;
3. sibling version runs/artifacts never leak across exact identities;
4. child collections are deterministically ordered and hard-bounded;
5. malformed optional payload/metadata does not crash or become invented structured truth;
6. dangling/missing artifact linkage degrades safely without filesystem probing;
7. freshness is exposed only from existing freshness authority, never inferred from timestamps/status;
8. no read path mutates persistent state;
9. frontend contains no production dossier fixtures or canonical React-owned dossier store;
10. visible exact-version selection preserves identity and unauthorized actions remain absent/unavailable.

## Expected implementation paths

Fresh verification immediately before coding remains mandatory. Expected touched boundary:

- `backend/app/modules/modeling/models.py`;
- `backend/app/modules/modeling/routes.py`;
- one existing modeling service/read seam, or one small cohesive projection module under the same owner;
- focused backend tests;
- existing frontend API read client/helpers;
- existing `/memory/models` surface/components and focused tests/browser proof.

`backend/app/core/schema.py` and migrations are outside readiness authority.

## Required terminal evidence

On one frozen exact implementation head:

- focused 113 backend tests;
- `cd backend && python -m pytest -q`;
- `cd backend && python -m ruff check app tests`;
- `cd frontend && npm run build`;
- exact-head browser/runtime proof for `/memory/models`, including truthful empty state and at least two exact versions using proof-only harness data when necessary;
- fresh ChatGPT semantic acceptance against this readiness/spec;
- any independent terminal review required or materially useful by current repository policy;
- no unresolved P0/P1/blocking P2 or material scope/authority finding.

Any head mutation invalidates affected evidence.

## Delivery decision

After this planning PR merges, the next authorized shared-authority action is a fresh-master registry reconciliation that moves only `113` from `planned` to `ready` if this packet remains valid. Runtime implementation must not begin before that reconciliation is on `master`.

Because 113 is a non-trivial code slice, the normal post-112 role split then applies: ChatGPT owns the exact implementation packet and acceptance/integration; GLM-5.3-Flash is the default bounded candidate/repair implementer unless fresh evidence makes direct ChatGPT work the safer minimal route.
