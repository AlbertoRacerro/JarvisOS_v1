# 140 CODING-FRONTEND-INTEGRATION-1 — readiness decision

Decision: READY, conditional on the canonical registry transition to `140 = ready` after this planning authority is merged.

Exact-master basis: `084c382451972081c23c4b05fd76cf1ba238b95c`

## Fresh evidence

- 118, 119, 120 and 123 are merged and remain the sole repository/runtime/pipeline/Jarvis Coding authorities.
- `/coding/repository` and `/coding/runtime` already exist in the production route map; no new frontend navigation architecture is needed.
- `/coding/repository` is still an explicit unavailable placeholder because the accepted 118 service is not wired into the browser-facing JarvisOS API surface.
- `/coding/runtime` currently renders `/system/info` only and truthfully leaves exact local SHA, remote SHA and alignment Unknown; 119 already exposes the accepted `/api/coding/runtime-truth` contract.
- 120 and 123 already expose `/api/coding/pipeline-state`, `/api/coding/actions/inspect` and `/api/coding/actions/suggest-modification`.
- 118 owns a bounded read-only operation allowlist, fixed GitHub hosts, path/ref/SHA validation, provider/error taxonomy, hard payload/collection bounds and safe GitHub URL construction. A minimal FastAPI projection may delegate to that service; the frontend must not duplicate or bypass those controls.
- Active 113 remains parked on its accepted exact-head real-browser `/memory/models` proof. Its domain/store/schema/routes are disjoint from 140. Shared `App.tsx` is an integration-owned file and any unavoidable Coding-only edit remains serialized under the global writer mutex.

## Readiness checks

- Scope is closed to wiring existing Coding operator surfaces to accepted 118/119/120/123 server-owned contracts.
- No new repository/runtime/pipeline/context/provider/GitHub/filesystem state authority is required.
- A minimal typed server HTTP projection for 118 is authorized only because the current service is not browser-facing; it must delegate to 118 and expose read-only accepted operations only.
- Browser GitHub/provider/filesystem/execution access and credentials remain forbidden.
- Runtime relation is rendered only from 119; client-side ancestry/alignment inference is forbidden.
- Pipeline state is rendered only from 120; client-side progression/merge truth is forbidden.
- Ordinary browsing remains context-neutral; explicit 111/123 action is the only accepted context insertion path.
- Suggest modification remains proposal/diff/plan only, bound to exact base SHA, target paths and context digest; stale/mismatched evidence refuses visibly.
- No Commit/Apply/Execute/Push/Create PR/Merge/STATUS mutation affordance may be introduced.
- Existing Coding route/shell/fusion surfaces must be reused; no general redesign, new global store or duplicate app/API architecture is authorized.
- 113 files/behavior must not be modified except an unavoidable shared `App.tsx` Coding-only switch/composition edit whose diff is mechanically isolated from `/memory/models`.
- 114 remains blocked until both 113 and 140 are merged and reconciled.

## Required implementation evidence

1. focused backend route/service tests for any 118 HTTP projection, including authorization, invalid/stale inputs, hard bounds/partial semantics, safe URLs and absence of mutation operations;
2. deterministic frontend/API tests proving requests go only through JarvisOS `/api/coding/*`, with exact identity propagation and no direct GitHub/provider requests;
3. deterministic Runtime proof that relation comes from 119 and error/unknown does not fall back to inferred Git identity;
4. deterministic pipeline proof that 120 state/staleness controls the UI and prior current state is invalidated;
5. deterministic context-neutral browsing and explicit-context tests;
6. deterministic 123 Suggest modification success/refusal tests preserving exact base SHA/paths/context digest and proposal-only semantics;
7. focused frontend tests and `npm run build`;
8. focused backend tests plus normal repository CI;
9. independent exact-head semantic review because the slice changes visible security/authority presentation and adds a server-to-browser integration seam;
10. exact-head real-browser proof of real `/coding/repository` and `/coding/runtime` behavior against JarvisOS backend responses. React-only mocked screenshots are insufficient.

## Decision

READY once the canonical registry row is transitioned to `ready` after this full-spec/readiness authority is merged. Implementation must remain one bounded integration slice. If fresh implementation work discovers that satisfying the accepted behavior requires a new durable store, GitHub/provider credential surface, generic RPC authority, execution/mutation authority, new global frontend architecture, or changes to 113's model-dossier domain, stop and re-derive authority rather than broadening 140.
