# 128 ARCHITECTURE-ENFORCEMENT-GATE-1 — readiness decision — 2026-08-30

**Decision: READY, subject to separate `STATUS.md` reconciliation.**

**Exact readiness base:** `8d07c8cb188b83b3ae9431b8153b1a7b6e1cccbe`  
**Definition authority:** PR #437  
**Full-spec authority:** PR #438 / `docs/specs/128-architecture-enforcement-gate-1.md`  
**Scheduling authority:** `docs/POST_112_PARALLEL_DELIVERY_PROFILE.md` hardening-first section 1A  
**Transport supplement:** `docs/GITHUB_CONNECTOR_COMPATIBILITY.md`  
**Coordination negative-authority contract:** `docs/COORDINATION_BUS_V2.md`

This readiness grants no implementation authority while `docs/specs/STATUS.md` remains `128=planned`. After this readiness merges, a separate docs-only registry reconciliation must move 128 to `ready`. No 113 implementation, 114–126 planning/implementation, jarvis-pr-attention integration, or 127+ implementation is authorized by this decision.

## Exact-master inventory method and freshness

The readiness audit is bound to exact Git tree `866e4345696b684a4c814ac475c6692800f4f863` at the SHA above. The first-party scan surface is the tracked repository source under:

- `backend/app/**/*.py` — product/backend runtime;
- `backend/tests/**/*.py` — deterministic test/fixture classification, never a production owner merely because a fixture contains a forbidden pattern;
- `frontend/src/**/*.{ts,tsx,js,jsx}` and frontend test/tooling JS/TS — product frontend and deterministic UI tooling;
- `scripts/**/*.{py,sh,ps1}` — repository-development tooling;
- `.github/workflows/*.{yml,yaml}` — structured workflow triggers, permissions and actions;
- root/config/launcher first-party shell/PowerShell/Python/JS material where present.

Generated build output, dependency/vendor material, caches and binary assets are classified as non-first-party executable source rather than silently scanned as architecture owners. The implementation scanner must enumerate from the working Git tree deterministically and must fail closed on source/parser uncertainty for covered file kinds.

The accepted hardening packet was freshly derived at `2a9563fc3016e4c2babff3bca9fc42f54e45902d`. Comparison from that SHA to this readiness base changes only repository documentation/governance files (`docs/GITHUB_CONNECTOR_COMPATIBILITY.md`, `docs/POST_112_PARALLEL_DELIVERY_PROFILE.md`, `docs/specs/128-architecture-enforcement-gate-1.md`, `docs/specs/STATUS.md`); no runtime, frontend, script or workflow path changed. The source findings below were then rebound by direct exact-SHA reads of the named owners/debt paths and all eight current workflow trigger surfaces.

## Frozen rule inventory and dispositions

### AE001 — persistence ownership

**Accepted owner:**

- `backend/app/core/database.py::open_sqlite_connection` → `sqlite3.connect(...)` is the single accepted raw SQLite connection chokepoint. It also owns the connection pragmas/row factory and lifecycle.

Imports of `sqlite3` used only for types/exceptions are not raw connection ownership. Ordinary services may receive/use a connection from `open_sqlite_connection`; AE001 must not reinterpret every SQL statement as a new database engine owner.

**Retained AE001 debt:** none identified on the accepted/current inventory. A new direct `sqlite3.connect`/aliased equivalent outside the exact accepted owner must fail; the implementation must not auto-baseline an unexpected current violation. If implementation's deterministic full-tree self-scan discovers an unlisted existing raw-connection owner, that is a material mismatch with this readiness and blocks merge pending a fresh disposition rather than silently adding an exception.

### AE002 — external dispatch ownership

Product external dispatch remains owned by the accepted AI/provider/egress execution boundary. Concrete provider implementation modules under `backend/app/modules/ai/providers/` may implement the network adapter itself; local Ollama runtime is a separately accepted loopback/local-AI owner and must never be classified as external routing authority merely because it uses HTTP locally. Repository-development review tooling is classified separately from product AI dispatch and does not gain product-provider authority.

**Exact retained legacy product debt owned by 129:**

1. `backend/app/modules/ai/deepseek_provider_smoke.py::run_provider_smoke` → direct `DeepSeekProviderAdapter.complete(...)`; classification `legacy_product_dispatch_debt`; owner/removal spec `129 EGRESS-SIDE-CHANNEL-CLOSURE-1`.
2. `backend/app/modules/ai/supervisor_public_test.py::run_supervisor_public_test` → `selection.adapter.complete(...)` after direct concrete provider selection in `_select_provider`; classification `legacy_product_dispatch_debt`; owner/removal spec `129 EGRESS-SIDE-CHANNEL-CLOSURE-1`.

These exceptions are exact path+symbol debt, not desirable provider owners and not directory exemptions. New direct provider adapter construction/dispatch outside accepted execution/provider/egress/local-AI owners or these two exact debt symbols must fail AE002. Generic external HTTP/provider SDK aliases in product source must not bypass the rule.

### AE003 — canonical mutation ownership

The first gate is deliberately a ratchet over statically provable mutation seams; it is not a semantic rewrite of the modeling stack.

**Accepted canonical/proposal owners:**

- `backend/app/modules/modeling/project_knowledge_owner.py` owns the 112 CAS/event-backed Project Knowledge canonical update helpers, including `_cas_update`, Requirement/Decision create/update/retire seams, assumption update and ModelSpec update;
- `backend/app/modules/modeling/parameter_lifecycle.py` remains the accepted 098 Parameter lifecycle/CAS owner;
- `backend/app/modules/project_knowledge/**` and the corresponding 112 owner composition may call those accepted transactional seams;
- `backend/app/modules/memory/service.py` may create proposal-only/calc evidence records under the existing 040 proposal/promotion boundary; proposal creation is not canonical promotion authority.

**Exact retained legacy public modeling mutation debt owned by 127:**

- `backend/app/modules/modeling/service.py::create_model_spec` direct canonical `model_specs` INSERT;
- `backend/app/modules/modeling/service.py::create_assumption` direct canonical `assumptions` INSERT;
- `backend/app/modules/modeling/service.py::create_parameter` direct canonical `parameters` INSERT;
- `backend/app/modules/modeling/service.py::update_requirement` direct `requirements` UPDATE without the 112 CAS/reconciliation seam;
- `backend/app/modules/modeling/service.py::create_simulation_run` direct run/evidence INSERT, retained as legacy ownership pending 127's required classification rather than silently absorbed into Project Knowledge.

`create_requirement` and `create_decision` in the same service delegate to the accepted transaction owner and are not debt merely because they remain compatibility entry points. Registered legacy mutating routes in `backend/app/modules/modeling/routes.py` are frozen as existing consumers; no new consumer of those direct-write surfaces is permitted before 127.

AE003 implementation must use a narrow AST/literal-SQL/import-call seam sufficient to reject new copies/new callers of protected canonical mutation patterns without guessing domain semantics from arbitrary function names. If a pattern cannot be proved statically without false authority, fail readiness/implementation review for that expansion rather than adding a broad wildcard.

### AE004 — Coordination Bus V2 negative authority

Current workflow inventory is exactly the eight files under `.github/workflows/` at the readiness base:

- `bluecad-real-tool-proof.yml` — `workflow_dispatch` / `pull_request`;
- `cheap-review.yml` — explicit `workflow_dispatch`;
- `ci.yml` — `pull_request` / push to master;
- `claude-review.yml` — pull-request `labeled`, explicitly maintainer-triggered by label;
- `codex-autopush.yml` — explicit `workflow_dispatch` with branch/PR inputs;
- `daily-development-continuation.yml` — schedule / `workflow_dispatch`;
- `project-knowledge-fast.yml` — `pull_request` / `workflow_dispatch`;
- `senior-review.yml` — explicit `workflow_dispatch`.

There is no current automatic `issue_comment` workflow trigger in that exact workflow set. Coordination Bus V2 was introduced as documentation/issue-comment coordination only and no repository-owned workflow/script gains authority merely from the marker. The scanner must reject any future automatic `issue_comment` or explicit `JARVIS_COORD_V2`/WORKPACK/CANDIDATE_PATCH parser that can reach code application, branch/file/STATUS/spec/readiness/review/label/merge/queue/shared-authority mutation. Inert marker text and the human/ChatGPT read-only helper append remain allowed and never count as acceptance or execution authority.

`codex-autopush.yml` and the continuation workflow retain their already-accepted explicit/manual or existing continuation authority; neither may be broadened by 128 and neither may consume V2 artifacts as authority.

## Exact exception configuration contract

Implementation may use one small machine-readable configuration file. Every retained exception must have exactly:

`{rule_id, exact_match, classification, owner_or_removal_spec, rationale}`

The accepted-owner entries and debt entries above are the maximum initial semantic baseline. No generated baseline or refresh command is allowed. Path-only parent-directory wildcards that can hide a new symbol are invalid when a path+symbol/call rule is practical. Unknown rule ids, duplicate/conflicting identities, missing owner/disposition or stale debt entries must fail closed.

## Allowed implementation paths

Only the following paths/boundaries are authorized for 128 implementation:

- `scripts/check_architecture_enforcement.py` — one deterministic local scanner/check;
- `configs/architecture_enforcement.json` — optional narrow exact owner/debt configuration; omit it if the same reviewability is clearer without config;
- `backend/tests/test_architecture_enforcement_gate.py` — focused scanner/config/fixture tests; fixture material must remain temporary/in-test and non-executing;
- `.github/workflows/ci.yml` — invoke the same local scanner command in normal PR CI;
- `docs/specs/STATUS.md` — lifecycle bookkeeping only.

No `backend/app/**`, production frontend, schema/migration, provider implementation, domain service, durable store, API or product behavior mutation is authorized.

## Required deterministic implementation behavior

1. Python ownership rules use AST and resolve common aliases / `from ... import ...` forms rather than regex-only import scanning.
2. Workflow checks use structured YAML or an equivalently deterministic parse; parser failure is failure, never clean evidence.
3. The scanner emits stable ordered diagnostics with rule id, path and symbol/pattern/trigger detail.
4. Test/fixture and repository-development classifications are explicit; neither becomes a blanket exemption.
5. Configuration validation is fail-closed and cannot self-refresh/broaden the baseline.
6. Running the same source/config twice produces stable equivalent findings.
7. The command is offline: no network, provider, secret, production data root or GitHub write.
8. V2 workpack/candidate text is inert data and has no apply/approval/queue/merge path.

## Focused acceptance matrix

Implementation must prove at minimum:

- accepted `database.py::open_sqlite_connection` raw SQLite owner → PASS;
- aliased `sqlite3.connect` in a non-owner fixture → FAIL AE001;
- accepted provider/egress implementation seam → PASS;
- aliased concrete provider/generic HTTP dispatch outside owner → FAIL AE002;
- each of the two exact 129 dispatch debt entries → PASS while still reported/attributable as debt;
- accepted Project Knowledge/Parameter/proposal mutation seams → PASS;
- new direct protected canonical mutation fixture/new caller → FAIL AE003;
- each exact 127 legacy modeling mutation debt entry → PASS while still attributable as debt;
- inert V2 marker/workpack fixture → PASS with no execution;
- workflow/script auto-consumption of V2 candidate from `issue_comment` into mutation → FAIL AE004;
- parent-directory wildcard or malformed/unknown/duplicate exception config → FAIL closed;
- same fixture tree scanned twice → stable ordered result.

## CI and exact-head terminal gates

Expected local command:

```text
python scripts/check_architecture_enforcement.py
```

Focused implementation checks:

```text
python scripts/check_architecture_enforcement.py --self-test
cd backend && python -m pytest -q tests/test_architecture_enforcement_gate.py
python -m ruff check app tests ../scripts/check_architecture_enforcement.py
```

Frozen implementation head must then satisfy the repository's normal full backend CI/pytest/Ruff, frontend build and BLUECAD deterministic proof required by current policy. No browser proof is required for 128 because production UI is outside the allowed path set and visible behavior must not change.

CI must call the same scanner implementation, preferably as an always-run PR step in `.github/workflows/ci.yml`; no separate CI-only rule engine is allowed.

## Minimum-necessary assessment

Acceptance target: prevent new ownership side channels before the next integration/hardening slices without changing product behavior.

- One standard-library-oriented scanner + one exact config only if needed + focused tests + one existing CI hook is sufficient.
- A service, database, policy daemon, generic linter framework, runtime refactor or automatic remediation is not necessary and is forbidden.
- Existing debt is visible and assigned to 127/129 rather than cleaned opportunistically.
- `jarvis-pr-attention` remains unintegrated until 128 itself merges.

## Residual risk / intentionally bounded coverage

Static enforcement cannot prove semantic correctness inside accepted owners, cannot prove arbitrary external actors will not manually copy bus text, and cannot safely infer every domain mutation intent from names. 128 therefore ratchets the exact statically provable seams above. 127/129 own the known runtime debt; later hardening may extend the gate only through fresh accepted authority.

If implementation discovers a current exact-master violation not represented above, it must not be auto-allowlisted. The implementation front returns to architecture/readiness review for an exact disposition.

## Readiness conclusion

At exact base `8d07c8cb188b83b3ae9431b8153b1a7b6e1cccbe`, the full 128 contract is implementable without runtime/product mutation. The exact accepted owners, known debt, narrow implementation boundary, deterministic test matrix, V2 negative-authority proof and terminal gates are sufficiently frozen.

**READY is accepted only as planning evidence in this PR.** `STATUS.md=planned` remains authoritative until a separate post-merge reconciliation changes it to `ready`. Implementation must not start before that reconciliation is on exact master.
