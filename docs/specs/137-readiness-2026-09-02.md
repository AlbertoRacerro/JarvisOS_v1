# 137 AE002-GENERIC-NETWORK-COVERAGE-1 — readiness decision

Decision: READY

Exact-master basis: `701b9fa86c20272b9e675961d6231144be111efb`

## Fresh evidence

The accepted 128 AE002 contract requires generic HTTP clients/provider SDK aliases not to bypass architecture enforcement. Fresh inspection of `scripts/check_architecture_enforcement.py` confirms the live scanner only recognizes `httpx`/`requests` request verbs plus provider-import `.complete()` calls. Fresh inspection of `backend/tests/test_architecture_enforcement_gate.py` confirms no deterministic coverage for the additional generic families frozen by spec 137.

This remains a live material correctness gap because first-party code can add direct dispatch through the uncovered families while the AE002 gate remains green.

The pre-readiness full-tree `urlopen` inventory was completed at exact-symbol granularity, not file granularity. The six affected files contain ten current-master symbols:

- `scripts/cheap_review.py::gh_request`
- `scripts/cheap_review.py::call_model`
- `scripts/cheap_review.py::call_model_with_retry`
- `scripts/daily_development_continuation.py::_request`
- `scripts/daily_development_continuation.py::_load_jwks`
- `scripts/daily_development_continuation.py::_post_comment`
- `scripts/verify_merge_authority.py::_request_json`
- `scripts/codex_pr_autopush.py::gh_request`
- `scripts/local_model_structured_output_probe.py::call_ollama_chat`
- `scripts/router_policy_local_responder.py::_stdlib_json_post_client`

The first eight are existing repository-control/review/079/134/022 network tooling. `router_policy_local_responder.py::_stdlib_json_post_client` is existing localhost-only routing evaluation tooling behind endpoint validation. The structured-output probe is different: its default is localhost, but fresh current-master inspection proves `call_ollama_chat(..., url=...)` accepts an arbitrary supplied URL and forwards it to `urllib.request`. It therefore becomes an exact retained-owner exception only after this implementation adds the bounded deterministic loopback URL check frozen below. The current architecture config accepts only exact `path::symbol` entries, validates target existence, and forbids wildcards. An eleventh newly exposed AE002 exception is not authorized and must stop implementation for re-derivation.

The scanner's import normalizer also has a concrete dotted-import bug relevant to this repair: `import urllib.request` binds the top-level name `urllib`, while the current alias map records that name as the full `urllib.request` module and can duplicate the `.request` component during dotted-call reconstruction. Current-head semantic review found the symmetric regression risk: `provider_import` currently derives its full provider-module knowledge from that same alias map. Replacing the mapping with Python-bound-name semantics without retaining a separate full imported-module inventory would make an unaliased `import app.modules.ai.providers.foo` stop triggering the existing `.complete()` AE002 path. Readiness therefore requires both facts to remain separate and tested.

The socket inventory confirms that connection-only scope is incomplete as a closure claim: both `socket.socket().sendto(payload, destination)` and, where supported, `socket.socket().sendmsg([payload], [], flags, destination)` can transmit externally without `connect` or `create_connection`. `sendto` and destination-bearing `sendmsg` are frozen into the required detector and fixture matrix.

Current-head review also proves a second destination escape at the two proposed localhost retained owners. Standard `urllib.request` follows HTTP redirects by default, so a request whose initial URL passed loopback validation can receive a 3xx with an external `Location` and issue a second request outside loopback while the actual network call sits inside an exact AE002 exception. The existing router-policy responder already validates its initial endpoint, but that does not close redirect egress. Readiness therefore requires deterministic redirect rejection at both retained localhost urllib transport boundaries, or equivalently fail-closed per-hop loopback validation. The minimum authorized design is redirect rejection; no generic network-policy or DNS/rebinding framework is needed.

A redirect-safe urllib implementation commonly uses a custom opener/redirect handler and dispatches through `OpenerDirector.open` instead of convenience `urlopen`. Replacing a detected `urlopen` with an unrecognized opener `.open` would itself create a new AE002 bypass. Readiness therefore freezes concrete `urllib.request` opener dispatch (`build_opener(...)` / same-file bound opener `.open`) into the scanner and hostile fixture surface, while keeping the two retained localhost functions as the same exact exceptions.

## Frozen implementation boundary

Authorized files:

1. `scripts/check_architecture_enforcement.py`
2. `backend/tests/test_architecture_enforcement_gate.py`
3. `configs/architecture_enforcement.json` only for the ten exact retained-owner AE002 entries inventoried above; no directory/wildcard/generic-symbol broadening.
4. `scripts/local_model_structured_output_probe.py` only for deterministic fail-closed loopback URL validation at `call_ollama_chat` and redirect rejection at its urllib transport boundary.
5. `scripts/router_policy_local_responder.py` only for redirect rejection at `_stdlib_json_post_client`; its existing localhost endpoint/path validation and higher-level responder semantics remain unchanged.
6. focused existing/new no-network tests for the local-probe URL boundary and both localhost urllib redirect boundaries.
7. `docs/specs/STATUS.md` for lifecycle bookkeeping.

No runtime `backend/app/**`, frontend, workflow, schema/migration, provider, credential, database, product, or domain mutation is authorized.

## Frozen design

- Extend the existing AST classifier; do not create another network-policy implementation.
- Correct import alias normalization so unaliased dotted imports follow Python's bound-name behavior and aliased/from-import forms still resolve to their full target.
- Preserve full imported-module targets separately from bound-name aliases so the existing provider-import `.complete()` detector remains effective for unaliased dotted provider imports.
- Add a small deterministic same-file constructor/binding map only where needed to recognize `urllib.request` openers, `urllib3.PoolManager`, `socket.socket`, `aiohttp.ClientSession`, and `http.client.HTTPConnection` / `HTTPSConnection` instance calls.
- Match only concrete frozen network methods/types, not generic `.open`, `.request` or `.connect` method names.
- Cover `urllib.request.urlopen` plus concrete opener `.open`, `urllib3` request dispatch, `socket.create_connection` plus socket-instance connect/connect_ex/sendto and destination-bearing sendmsg, aiohttp session request verbs, `websockets.connect`, and http.client connection request/connect.
- Preserve current accepted-owner behavior and exact-exception semantics.
- Before excepting `scripts/local_model_structured_output_probe.py::call_ollama_chat`, parse its supplied URL locally and fail closed unless the host is literal `localhost` or a standard-library-classified loopback IP; reject malformed/missing hosts, user-info ambiguity, and non-loopback hosts before request construction/dispatch. No DNS/network lookup is part of validation.
- Before excepting either localhost urllib transport, ensure HTTP redirects are not automatically followed. A 3xx returned from the validated loopback endpoint must terminate as a typed/local transport failure before a second request can be emitted. An equivalently fail-closed per-hop loopback check is acceptable only if it is smaller and deterministically testable; do not build a generic redirect-policy service.
- Preserve `router_policy_local_responder.py`'s existing strict initial endpoint validation (`http`, localhost host, `/api/generate`, no credentials/query/fragment); 137 only closes redirect escape at its stdlib transport seam.
- If redirect rejection uses a custom urllib opener, its `.open` call remains detectable by AE002 outside accepted/exact-exempted owners; do not solve one bypass by introducing another scanner blind spot.
- The ten inventoried existing symbols may be exact exceptions only subject to the two localhost destination guards above; no additional exception is implicitly authorized.
- Dynamic/interprocedural dispatch not resolved by this bounded static pattern set remains residual risk rather than justification for a taint framework.

## Deterministic acceptance evidence required

Focused fixtures must independently fail AE002 for every frozen family and alias/from-import form that applies, plus prove:

- unaliased `import urllib.request` resolves correctly rather than duplicating the dotted path;
- `urllib.request.urlopen` and a representative `build_opener(...)` / same-file bound opener `.open` outside an accepted owner both produce AE002;
- unaliased dotted `app.modules.ai.providers...` import followed by `.complete()` outside an accepted owner still produces AE002 after alias normalization;
- `socket.sendto` and destination-bearing `socket.sendmsg` are detected without a preceding connect;
- accepted owners remain accepted;
- each of the ten inventoried retained symbols is suppressible only by its exact symbol entry, with the two localhost transports guarded before their exceptions are valid, including if redirect-safe urllib opener dispatch is used;
- local probe default/explicit loopback URLs are accepted while representative external host, malformed/missing host, and user-info ambiguity are rejected before any dispatch; the test must not make a live network call;
- both localhost urllib transports reject a loopback response that attempts to redirect to a representative external `Location`, proving no follow-up request is issued outside loopback; use deterministic no-live-network fixtures/handlers;
- an exact exception does not cover a sibling symbol;
- unrelated same-named `.open`, `.request`, and `.connect` methods are not false positives;
- repeated scans produce stable diagnostics;
- full-tree architecture enforcement is green without any eleventh new AE002 exception.

Required commands on the implementation head:

- `python scripts/check_architecture_enforcement.py --self-test`
- focused architecture-enforcement tests
- focused no-network localhost-tool URL/redirect-boundary tests
- normal required repository CI / architecture gate

Because this changes an architecture enforcement gate, merge additionally requires independent exact-current-head semantic review and the normal bounded PROUD decision.

## Minimum-necessary / risk decision

READY. The implementation remains bounded: one existing static scanner, its focused tests/config, and two existing evaluation/control-script urllib boundaries whose proposed exact exceptions are truthful only if destination escape is closed. The current-head review failure modes are explicitly closed before implementation authority: dotted-import correction must preserve provider detection, constructor/datagram bypasses must be covered, redirect-safe opener dispatch must remain visible to AE002, the structured-output probe must reject arbitrary external initial URLs, both localhost urllib clients must reject redirect escape, and every pre-existing `urlopen` owner is classified before the widened full-tree gate runs. No human product/scope decision, credential, spend, migration, application-runtime behavior, or new egress owner is required.
