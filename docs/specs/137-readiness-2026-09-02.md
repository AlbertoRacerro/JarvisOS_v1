# 137 AE002-GENERIC-NETWORK-COVERAGE-1 — readiness decision

Decision: READY

Exact-master basis: `701b9fa86c20272b9e675961d6231144be111efb`

## Fresh evidence

The accepted 128 AE002 contract requires generic HTTP clients/provider SDK aliases not to bypass architecture enforcement. Fresh inspection of `scripts/check_architecture_enforcement.py` confirms the live scanner only recognizes `httpx`/`requests` request verbs plus provider-import `.complete()` calls. Fresh inspection of `backend/tests/test_architecture_enforcement_gate.py` confirms no deterministic coverage for the additional generic families frozen by spec 137.

This remains a live material correctness gap because first-party code can add direct dispatch through the uncovered families while the AE002 gate remains green.

The pre-readiness full-tree inventory was also completed rather than deferred to implementation. Widening `urllib.request.urlopen` detection will expose exactly six retained current-master symbols:

- `scripts/cheap_review.py::gh_request` — repository review/control-plane GitHub API;
- `scripts/daily_development_continuation.py::_request` — 079 continuation GitHub API reader;
- `scripts/verify_merge_authority.py::_request_json` — 134 read-only GitHub verifier;
- `scripts/codex_pr_autopush.py::gh_request` — bounded 022/Codex PR actuator GitHub API call;
- `scripts/local_model_structured_output_probe.py::call_ollama_chat` — localhost-only evaluation probe;
- `scripts/router_policy_local_responder.py::_stdlib_json_post_client` — localhost-only responder transport behind endpoint validation.

The existing architecture config accepts only exact `path::symbol` entries, validates target existence, and forbids wildcards. Readiness therefore authorizes exactly those six AE002 retained-owner entries so the widened detector can distinguish already-sanctioned repository/local tooling from new first-party egress. A seventh newly exposed AE002 exception is not authorized and must stop implementation for re-derivation.

The socket inventory also confirms that the original connection-only scope was incomplete as a closure claim: `socket.socket().sendto(payload, destination)` can transmit externally without `connect` or `create_connection`. `sendto` is now frozen into the required detector and fixture matrix.

## Frozen implementation boundary

Authorized files:

1. `scripts/check_architecture_enforcement.py`
2. `backend/tests/test_architecture_enforcement_gate.py`
3. `configs/architecture_enforcement.json` only for the six exact retained-owner AE002 entries inventoried above; no directory/wildcard/generic-symbol broadening.
4. `docs/specs/STATUS.md` for lifecycle bookkeeping.

No runtime `backend/app/**`, frontend, workflow, schema/migration, provider, credential, database, product, or domain mutation is authorized.

## Frozen design

- Extend the existing AST classifier; do not create another network-policy implementation.
- Reuse import alias normalization.
- Add a small deterministic same-file constructor-binding map only where needed to recognize `urllib3.PoolManager`, `socket.socket`, `aiohttp.ClientSession`, and `http.client.HTTPConnection` / `HTTPSConnection` instance calls.
- Match only concrete frozen network methods/types, not generic `.request` or `.connect` method names.
- Cover `urllib.request.urlopen`, `urllib3` request dispatch, `socket.create_connection` plus socket-instance connect/connect_ex/sendto, aiohttp session request verbs, `websockets.connect`, and http.client connection request/connect.
- Preserve current accepted-owner behavior and exact-exception semantics.
- The six inventoried existing symbols may be exact exceptions; no additional exception is implicitly authorized.
- Dynamic/interprocedural dispatch not resolved by this bounded static pattern set remains residual risk rather than justification for a taint framework.

## Deterministic acceptance evidence required

Focused fixtures must independently fail AE002 for every frozen family and alias/from-import form that applies, plus prove:

- `socket.sendto` is detected without a preceding connect;
- accepted owners remain accepted;
- each of the six inventoried retained symbols is suppressible only by its exact symbol entry;
- an exact exception does not cover a sibling symbol;
- unrelated same-named methods are not false positives;
- repeated scans produce stable diagnostics;
- full-tree architecture enforcement is green without any seventh new AE002 exception.

Required commands on the implementation head:

- `python scripts/check_architecture_enforcement.py --self-test`
- focused architecture-enforcement tests
- normal required repository CI / architecture gate

Because this changes an architecture enforcement gate, merge additionally requires independent exact-current-head semantic review and the normal bounded PROUD decision.

## Minimum-necessary / risk decision

READY. The implementation is additive, deterministic, repository-static, reversible, and confined to the existing scanner/test/config owners. The two principal failure modes are now explicitly closed at readiness: constructor/datagram bypasses must be covered, and pre-existing `urlopen` owners are classified before the widened full-tree gate runs. No human product/scope decision, credential, spend, migration, or runtime behavior change is required.
