# 100c final capability and interaction ownership — exact master `f50eb0a`

Audited source SHA: `f50eb0a1f5b246d5e6f6eabab3a033e9a8c5a5c5`.

This artifact closes the two exhaustive preservation gates of `100c FINAL-PRODUCT-DIRECTION-AUTHORITY-0`: every row of the final operator capability matrix receives a final owner/disposition, and every preserved user-facing interaction/action/state family receives an owner or concrete deferral. It is planning authority only; no runtime implementation is granted here.

Owner IDs below refer to the canonical queue in `docs/specs/100c-queue-rederivation-2026-08-28.md`. `100f/100g` always retain default visual/composition ownership for the eleven canonical surfaces. A later visible mutation must preserve the exact reference identity/browser-proof obligations in the approved 2026-08-27 manifest.

## 1. Exhaustive capability-matrix disposition

### Global shell and truth rules

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Primary workspace rail | `100f/100g REFERENCE/RUNTIME OWNER`; later rows may activate data/actions only; no IA redesign. |
| Shared appearance language | `100/100f/100g`; provider/system data additions in `124`; no independent slice. |
| Jarvis context/actions | generic mechanics `111`; Project Knowledge actions `121`; Development `122`; Coding `123`; later engineering adapters after 103–110. |
| Proposal vs canonical state | existing `040/041/042/054/090/091/097` authority preserved; `111` supplies generic Jarvis contract; COMMIT remains domain-owned. |

### Design — Process / BLUECAD

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Design peer modes | `100f/100g REFERENCE/RUNTIME OWNER`; no new slice. |
| Design context strip | generic exact-ref/context contract `111`; truthful existing modeling/run/proposal/source reads remain current owners; late Design rows may enrich. |
| Process editing surface | `REFERENCE_ONLY` until late `103–110` plus reordered `093` establish real Process authority; no fake topology/solver. |
| Process Properties | existing `071b/097` where already supported; future process-specific editable fields wait for late engineering owner. |
| BLUECAD editing surface | existing merged BLUECAD owners `005/006/085/092/058c/097` preserved; no reimplementation. |
| Design proposals | existing proposal/model owners + `111` generic Jarvis contract; later engineering-domain adapter after 103–110. |

### Memory — Project Basis

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Project search left pane | `115 PROJECT-SEARCH-1`; read projection only. |
| Project Basis dossier | `112 PROJECT-KNOWLEDGE-CORE-1`; reuses canonical engineering-record owners. |
| Value / rule / threshold display | read projection owned by `112`, preserving existing Requirement/Parameter semantics. |
| Proposed basis change | domain change-set/write authority `112`; Jarvis proposal/context adapter `121`. |
| Deterministic immediate re-evaluation | `112`; deterministic dependency/evidence classifier, never LLM confidence. |
| Recalculation required | `112` classifies/references affected domain; actual late Process/BLUECAD EXECUTE remains owning engineering authority. |
| Working revisions | `112`; exact parent/change-set identity, immutable lineage. |
| Reconciliation | `112`; atomic exact-revision promotion with required validation state and history preservation. |

### Memory — Models

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Exact model/version dossier | `113 MODEL-DOSSIER-1`; aggregation/read only over existing version/run/artifact/source owners. |
| Overview-first disclosure | `100f/100g` presentation plus truthful summaries from `113`; no separate state. |
| Large-section scaling | `113` read/query contract plus presentation in existing frontend; no new canonical store. |
| Current vs working revision identity | write/revision truth `112`; dossier projection `113`. |

### Memory — Literature

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Compact source/file list | `114 LITERATURE-KNOWLEDGE-1`; frontend composition remains `100f/100g`. |
| Inline multi-expand | `100f/100g PRESENTATION`; source reads `114`; no canonical mutation. |
| Extracted knowledge | `114`; exact source/document/claim/datum/citation/location/provenance. |
| Bounded preview | `114` over existing file/source safe-read boundary; unsupported remains unavailable. |
| Open full source | `114` NAVIGATE contract using safe real URL/file owner. |
| Web finding promotion | `114` intake/provenance plus existing proposal spine; Jarvis research/extraction remains proposal-only through `121`. |

### Development — Roadmap / Timeline / Execution status

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Canonical Roadmap work item | `116 ROADMAP-CALENDAR-1`; one stable work-item identity. |
| Timeline | `116` read/write owner; `100f/100g` presentation. |
| Execution status under Timeline | `116`; projection of same Roadmap status field, no Board store. |
| Ready state | `116`; deterministic dependency gate. |
| Drag/status transition | `116`; COMMIT on same item, Done blocked by deterministic done-when where knowable. |
| Manual work item | `116` COMMIT under operator authority. |
| Work item details | `116` projection plus exact links to other domain owners. |

### Development — Calendar

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Day / Week / Month / Agenda | `116`; same Calendar event/time-allocation owner. |
| Actual time block | `116`; exact date/start/end/time-zone and event type. |
| Roadmap link | `116`; zero/one/many time blocks link to one work item without identity duplication. |
| Schedule work from Roadmap | `116`; prefill is presentation, persistence is explicit COMMIT. |
| Create from time cell | `116`; presentation prefill then explicit COMMIT. |
| Effort metrics | `116`; derive only from real Roadmap/Calendar data, otherwise Unknown. |
| Jarvis scheduling | `122` PROPOSE over `116` READ; acceptance remains `116` COMMIT. |

### Development — Brainstorm

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| RAW capture | `117 BRAINSTORM-1`; original content/attachment identity preserved. |
| Raw state | `117`; NEW/DISCUSSED/RECONCILED/SUPERSEDED lineage. |
| Reconciled idea | `117`; versioned synthesis and provenance. |
| Inline multi-expand | `100f/100g PRESENTATION` over `117` reads. |
| Explicit Jarvis context basket | generic mechanics `111`; Development-domain admissible refs/actions `122`. |
| Reconciliation proposal | `122` PROPOSE; `117` explicit acceptance/COMMIT. |
| Add to Roadmap | `117 -> 116` explicit promotion bridge; Jarvis-created change is proposal until accepted. |
| Promote to Design | `117` explicit promotion proposal into later Design authority; no direct model mutation. |
| Promote to Coding | `117 -> 120/123` development proposal/lifecycle; no direct code mutation. |
| Record speech | `DEFER_TRIGGERED`; reopen only after `117` and bounded media/privacy path; every transcription inference must use canonical `run_ai_task`/accepted AI spine and ledger, never direct media→provider. |

### Settings — Appearance / AI / System

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Appearance | existing `029/100/100f/100g`; no new state owner. |
| Provider identity | `124 PROVIDER-SETTINGS-GENERIC-1`, reusing `015/018/082/094` and policy owners. |
| Credential update/test | `124` facade over `082` secure storage/provider boundary; secret never returned to normal UI/logs. |
| Local AI | `124` truthful read over existing local/provider runtime; no invented availability. |
| Orchestration policy | `124` projection/config over current routing/egress/budget; Hermes remains frozen/deferred, UI label cannot create it. |
| Budget/usage | `124` over existing budget/job/provider accounting; unsupported metrics remain Unknown. |
| System diagnostics | `124` read projection over existing health/config/database/runtime services; no synthetic Healthy/Ready. |

### Coding — Repository

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Remote repository identity | `118 CODING-REPOSITORY-TRUTH-1`; server-side remote truth only. |
| Active development lifecycle | `120 DEVELOPMENT-PIPELINE-STATE-1`; exact repository/head/gate/review state. |
| Current work | `120` projection from canonical STATUS/spec/PR state. |
| Repository Inspector search | `118`; path/literal/ID read projection, no duplicate repo truth store. |
| Artifact preview | `118`; bounded exact ref/path/blob preview with type/size allow-list. |
| Architecture inspection | current Markdown/SVG artifacts through `118`; semantic graph is `DEFER_TRIGGERED` if artifact inspection later proves insufficient. |
| Open on GitHub | `118` NAVIGATE using exact safe repository URL. |
| Add to Jarvis context | generic `111` context mechanics plus Coding admissible refs `123`; selection alone is inert. |
| Suggest modification | `123` PROPOSE only; enters `120` development lifecycle, never direct save. |
| Coding knowledge | `118` exact repository artifacts + `123` explanation/context; generated explanation is not source authority. |
| Branch/worktree implementation | future/accepted development control plane behind `120/123`; isolated branch/worktree only; no live-running-code target. |

### Coding — Runtime

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Local actually executed identity | `119 CODING-RUNTIME-TRUTH-1`; observed local path/ref/SHA/dirty/runtime identity. |
| Latest approved GitHub identity | remote truth `118`, selected target projected by `119/125`; never inferred as local. |
| Alignment state | `119`; deterministic exact-history classification aligned/local-behind/divergent/unknown. |
| Semantic delta | `119` evidence-backed compare over `118`; LLM prose may summarize only bounded evidence and cannot become fact. |
| Inspect delta evidence | `118` exact commit/file preview reached from `119`. |
| Service health | `119` over observed health/system boundaries; no hard-coded Healthy. |
| Safe update preparation | `125 SAFE-SELF-UPDATE-1`; exact target, dirty guard, backup/migration/build/smoke/rollback plan. |
| Safe update execution | `125`; explicit EXECUTE, restart/health/rollback; no blind pull/hot swap. |

### Coding — future integrated terminal

| Capability-matrix row | Final owner/disposition |
| --- | --- |
| Real terminal session | `126 LOCAL-TERMINAL-PTY-1`, security-gated; unavailable until no-secret PTY display/process boundary is proven. |
| Terminal / Logs tabs | `126` terminal read/interaction + existing log owner; presentation remains 100f/100g-compatible. |
| Session cwd/history/scroll | `126`; session-scoped noncanonical state behind typed backend. |
| Ctrl+C / interrupt | `126` backend-mediated EXECUTE. |
| Open terminal here | `118 -> 126` bridge; backend validates cwd/path and denies/protects secret paths. |
| Send output to Jarvis | `126` bounded secret-safe output then `111/123` explicit CONTEXT with separate egress policy. |
| Jarvis command proposal | `123` PROPOSE; `126` may Insert/Copy as presentation; no automatic execution. |
| High-risk command confirmation | `126` backend policy/classification + explicit operator confirmation. |
| Secret-safe terminal process | `126`; scrubbed/minimum environment and backend-owned secret-safe display/isolation. Failure to prove this keeps arbitrary PTY unavailable. |
| Offline CI | `126`; replaceable/fake PTY adapter, no live PowerShell/network/provider dependency. |

### Cross-surface completion obligations

| Preserved cross-surface obligation | Final owner chain |
| --- | --- |
| Project Basis changes affect exact model working revisions/revalidation | `112 -> 113`, with actual engineering recomputation delegated to the owning Process/BLUECAD evaluator. |
| Literature/search records can be linked and explicitly added to Jarvis context | `114/115 -> 111/121`; browsing itself remains READ/PRESENTATION only. |
| Roadmap and Calendar use one work-item/time-allocation model without conflating project window and occupied time | `116`. |
| Brainstorm promotion reaches Roadmap/Design/Coding only through explicit authority bridges | `117 -> 116`, late Design authority, or `120/123`; never silent promotion. |
| Repository remote truth and Runtime local truth remain independent and comparable | `118 -> 119`; update mutation only `125`. |
| Repository/Runtime exact refs can be explicitly added to Jarvis | `118/119 -> 111/123`; no implicit context. |
| Terminal output can enter Jarvis only after secret-safe bounded selection | `126 -> 111/123` plus canonical egress policy. |
| Settings/provider state remains policy/credential scoped and separate from orchestration implementation | `124` over existing `015/018/021/059b/061a/082/094`; Hermes deferred. |
| Design Process and BLUECAD preserve approved affordances while unavailable actions remain truthful | `100f/100g` presentation; existing BLUECAD owners; late `103–110/093` Process authority. |

## 2. Exhaustive interaction/action/state ownership

The seven action classes are immutable: `PRESENTATION | READ | CONTEXT | PROPOSE | COMMIT | EXECUTE | NAVIGATE`. The mapping below binds the preserved interaction families; an unavailable action remains its original class and is not silently downgraded.

### Global shell

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Navigate exactly Design/Memory/Development/Coding/Settings and their approved peer tabs | NAVIGATE/PRESENTATION | `100f/100g`; no later IA redesign. |
| Select/open/browse records without adding Jarvis context | PRESENTATION/READ | owning domain read service; `111` explicitly forbids implicit context. |
| Add/remove exact visible refs to Jarvis context | CONTEXT | `111`; domain admissibility/actions in `121–123`; stale refs fail closed. |
| Preview current Jarvis context with digest/source manifest | READ/CONTEXT | `111`, reusing existing context-pack/AI spine. |
| Jarvis/model response remains explanation/proposal until accepted | PROPOSE | existing AI/proposal spine through `111`; domain COMMIT stays separate. |
| Stale exact target invalidates proposed action | PROPOSE/COMMIT guard | generic stale-ref contract `111` plus domain owner CAS/version checks. |
| Loading/error/unknown/unavailable states never reuse fixtures or infer success | READ/PRESENTATION | every owning read contract; cross-cutting acceptance requirement. |

### Design / Process

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Equipment/object selection drives exact Properties | PRESENTATION + READ | existing truthful read seams where available; later Process owner after bakeoff. |
| Property edit | COMMIT | existing `071b/097` only for accepted fields; future Process fields wait for late engineering owner. |
| Select/Pan/Multi-select/Fit/Zoom presentation tooling | PRESENTATION | `100f/100g`/existing viewport owners. |
| Add equipment/Connect/Disconnect/Duplicate/Delete/Undo/Redo/Auto-layout | COMMIT/EXECUTE as semantically applicable | `DEFER_TRIGGERED` until real Process topology/change authority after late engineering sequence; controls stay truthful unavailable. |
| Validate/Solve | EXECUTE | late evaluator/process owners `103–110/093`; no frontend/local fake. |
| Jarvis engineering change | PROPOSE | `111` generic + future Design-domain adapter; acceptance remains Process/model owner. |

### Design / BLUECAD

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Feature/object selection drives CAD Properties | PRESENTATION + READ | existing `085/092/058c` and BLUECAD services. |
| Constraints/measurement/validation/analysis/messages docks | PRESENTATION/READ/EXECUTE only where already supported | existing BLUECAD owners; unsupported actions stay unavailable. |
| Export/study/version navigation | READ/NAVIGATE/EXECUTE only where accepted | existing BLUECAD/model/run/artifact owners. |
| Jarvis CAD changes | PROPOSE | existing `097` where applicable plus `111`; CAD/model owner accepts. |

### Memory / Project Basis

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Dossier section expand/collapse and compact scanning | PRESENTATION | `100f/100g`; records from `112`. |
| Search Project Knowledge | READ/NAVIGATE | `115`. |
| Jarvis proposes one/bounded batch basis changes | PROPOSE | `121` over `112` exact refs/change contract. |
| Inspect exact diff/batch | READ | `112/121`. |
| Approve all exact displayed batch | COMMIT | `112`; atomically creates/advances working revision, never overwrites reconciled parent. |
| Criterion-only deterministic re-evaluation | EXECUTE/deterministic validation | `112`; automatic after accepted change when exact outputs suffice. |
| Recalculation-required classification and Validate-in-domain link | READ/NAVIGATE | classifier `112`; actual domain EXECUTE later owning Process/BLUECAD authority. |
| Reconcile / Make current exact working revision | COMMIT | `112`; blocks unresolved mandatory validation and preserves immutable lineage. |

### Memory / Models

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Select exact model/version/working vs reconciled revision | PRESENTATION + READ | `112/113`. |
| Expand/collapse sections, Collapse all, internal filter/pagination/scroll | PRESENTATION | `100f/100g` over `113` bounded reads. |
| Inspect version-scoped result/run/artifact/source/validation | READ/NAVIGATE | `113` aggregation over existing owners. |
| Working-revision mutation/reconciliation | COMMIT | `112`, not `113`. |

### Memory / Literature

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Toggle source row inline; keep multiple rows expanded | PRESENTATION | `100f/100g`; source data `114`. |
| Read extracted claims/values/locations/used-by/provenance | READ | `114`. |
| Bounded PDF/image/text preview | READ/PRESENTATION | `114`; unsupported explicit. |
| Open full source in browser/native viewer | NAVIGATE | `114` safe real target. |
| Web/research/extraction result | PROPOSE | `121` + `114` intake/provenance; never canonical by browsing alone. |
| Add selected source/claim to Jarvis context | CONTEXT | `111/121`; explicit removable ref. |

### Development / Roadmap Timeline

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Zoom/filter/focus timeline and expand Execution status | PRESENTATION | `100f/100g`, values from `116`. |
| Add/Edit/Delete manual work item | COMMIT | `116`, same stable item identity. |
| Status transition including Ready/In progress/Blocked/Done | COMMIT | `116`; deterministic dependency/done-when gates apply. |
| Inspect details/dependencies/acceptance/links/progress | READ/NAVIGATE | `116` plus linked domain owners. |
| Jarvis creates/changes work item | PROPOSE | `122`; acceptance `116`. |

### Development / Calendar

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Day/Week/Month/Agenda switching | PRESENTATION | `100f/100g` over `116`. |
| Click time cell / Schedule work prefill | PRESENTATION | `116`; no persistence yet. |
| Create/Edit/Delete exact time block | COMMIT | `116`. |
| Link Roadmap item to zero/one/many Calendar blocks | COMMIT/READ | `116`, both identities preserved. |
| Jarvis proposes exact slot from availability/dependencies | PROPOSE | `122`; acceptance `116`. |

### Development / Brainstorm

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Capture original RAW text/attachments | COMMIT | `117`; original remains immutable lineage. |
| Open/expand/multi-expand idea | PRESENTATION/READ | `100f/100g` + `117`; no context side effect. |
| Change RAW lineage state via accepted workflow | COMMIT | `117`; DISCUSSED != SUPERSEDED. |
| Add/Remove idea/raw/project refs to Jarvis context | CONTEXT | `111/122`. |
| Discuss/Reconcile with Jarvis, proposed synthesis/diff | PROPOSE | `122`; accepted reconciliation `117` COMMIT. |
| Add to Roadmap | PROPOSE/COMMIT bridge | `117 -> 116`; explicit acceptance according to source/action origin. |
| Promote -> Design | PROPOSE | `117` into late Design proposal/change path; no direct model COMMIT. |
| Promote -> Coding | PROPOSE | `117 -> 120/123`; no direct code mutation. |
| Record speech | EXECUTE/PROPOSE | `DEFER_TRIGGERED` until media + canonical AI execution/ledger path exists. |

### Coding / Repository

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Read remote repo/branch/SHA/PR/check/review/current work | READ | `118/120`; exact remote evidence. |
| Search Repository Inspector | READ | `118`. |
| Select/preview Markdown/code/config/SVG/image/artifact | PRESENTATION + READ | `118`. |
| Open on GitHub | NAVIGATE | `118`, exact real URL. |
| Add exact artifact/PR/spec/ref to Jarvis | CONTEXT | `111/123`; browsing alone inert. |
| Suggest modification | PROPOSE | `123`; produces diff/plan/reason/affected files, then enters `120`. |
| Proposal -> Plan -> Implementation -> Tests -> Review -> Reconciliation -> Merge | COMMIT/EXECUTE lifecycle | `120` projects state; concrete Git/repository mutation remains accepted server-side development authority, isolated branch/worktree, never browser direct. |
| Architecture inspect/expand | READ/PRESENTATION | `118`; semantic graph editing deferred. |

### Coding / Runtime

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Observe local executed SHA/path/branch/dirty/service health | READ | `119`. |
| Observe latest approved remote SHA and alignment | READ | `118/119`. |
| Inspect semantic delta/underlying commits/files | READ/NAVIGATE | `119 -> 118`. |
| Prepare update | READ/PROPOSE-like preparation then explicit EXECUTE boundary | `125`; exact target/dirty/backup/build/smoke/rollback plan. |
| Execute update/restart/health/rollback | EXECUTE | `125`; separate from observation. |
| Open Terminal/Logs tab | PRESENTATION/READ | terminal only after `126` readiness; logs existing owner. |
| Create/use terminal session, stdin, resize, Ctrl+C, cwd | EXECUTE | `126` only after security proof. |
| Open terminal here from repository path | NAVIGATE/EXECUTE | `118 -> 126` with validated cwd. |
| Send selected safe terminal output to Jarvis | CONTEXT | `126 -> 111/123`, bounded after secret-safe display and egress checks. |
| Jarvis command suggestion / Insert / Copy | PROPOSE/PRESENTATION | `123/126`; execution always explicit. |
| Run destructive/high-risk command | EXECUTE | `126` policy + explicit confirmation. |

### Settings

| Interaction/state family | Action class | Final owner/disposition |
| --- | --- | --- |
| Appearance System/Light/Dark/accent/custom HEX | PRESENTATION/local preference COMMIT | existing `029/100/100f/100g`; no domain authority effect. |
| Read provider/integration configuration/status/catalogue | READ | `124` over existing provider/security owners. |
| Save/replace/remove credential | COMMIT | `124 -> 082`; no secret redisplay. |
| Test connection / refresh models | EXECUTE | `124` through provider gateway/policy; never direct frontend provider call. |
| Read/change orchestration policy, fallback, sensitivity permissions | READ/COMMIT | `124` over existing policy; Hermes controls unavailable until actual Hermes authority exists. |
| Read/set budget/limits | READ/COMMIT | `124` over existing budget owner. |
| Read System diagnostics | READ | `124` over observed health/config/database/runtime owners. |
| Reload/refresh system state | READ/EXECUTE only if backend owner exists | `124`; otherwise unavailable. |

## 3. Eleven canonical surfaces — final ownership and proof obligation

| Surface | Default render/composition | READ | CONTEXT / PROPOSE | COMMIT / EXECUTE | NAVIGATE | Future browser-proof trigger |
| --- | --- | --- | --- | --- | --- | --- |
| Design / Process | `100f/100g` | existing truthful reads; late engineering | `111` + future Design adapter | late `103–110/093` only where real | exact contextual owners | any visible activation/change against canonical Process HTML/hash/viewport |
| Design / BLUECAD | `100f/100g` + existing BLUECAD | existing BLUECAD/model/run | `111` + existing/future Design adapter | existing accepted BLUECAD actions | exact model/run/artifact | any visible change against BLUECAD HTML/hash/viewport |
| Memory / Project Basis | `100f/100g` | `112/115` | `111/121` | `112` | linked model/source/domain | visible activation/change against Project Basis reference |
| Memory / Models | `100f/100g` | `113` + existing owners | `111/121` | `112` revision/reconciliation | exact run/artifact/source | visible activation/change against Models reference |
| Memory / Literature | `100f/100g` | `114/115` | `111/121` | `114` only for explicitly specified source/import mutations | `114` | visible activation/change against Literature reference |
| Development / Roadmap | `100f/100g` | `116` | `111/122` | `116` | linked domain/calendar | visible activation/change against Timeline reference |
| Development / Calendar | `100f/100g` | `116` | `111/122` | `116` | linked Roadmap/domain | visible activation/change against Calendar reference |
| Development / Brainstorm | `100f/100g` | `117` | `111/122` | `117` | promotions/linked sources | visible activation/change against Brainstorm reference |
| Coding / Repository | `100f/100g` | `118/120` | `111/123` | isolated development authority behind `120`; no browser direct write | `118` | visible activation/change against Repository reference |
| Coding / Runtime | `100f/100g` | `118/119` | `111/123` | `125`, and `126` only after security readiness | `118/126` | visible activation/change against Runtime reference |
| Settings | `100f/100g` | existing + `124` | no persistent Jarvis sidecar required | existing appearance + `124` backend-mediated settings actions | provider/system details | visible activation/change against Settings reference |

## 4. Explicit deferrals/rejections preserved

- Hermes `066–068` remains frozen; `060` is not an early implementation front. `111` must work with the current AI execution spine and expose a replaceable future Hermes adapter boundary only.
- `080` remains frozen.
- `101` standalone write-unification is superseded by the broader-but-single-owner `112`; valid 040/042/071b/098 obligations are mandatory inputs, not discarded.
- `055` standalone Project view is superseded; final Memory composition plus 112–115 provide the intended projection without another store.
- `064` semantic literature RAG is trigger-deferred behind 114/115 evidence; no default vector store.
- `069` memory consolidation is deferred while Hermes is frozen.
- Brainstorm speech remains trigger-deferred after 117 and must use the canonical AI execution/ledger path.
- Semantic architecture graph/editing is trigger-deferred; current architecture artifacts are inspected by 118.
- Arbitrary PTY remains unavailable until 126 proves local-only access, lifecycle, environment/secret isolation, secret-safe frontend output, protected cwd/path validation, high-risk confirmation, Jarvis-context scrubbing and offline fake tests.
- Process topology/Solve/Validate editing authority remains late and unavailable until the process/evaluator/upstream sequence provides a real owner; canonical affordances are preserved, not reinterpreted.
