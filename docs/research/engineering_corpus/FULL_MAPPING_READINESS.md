# Full Mapping Readiness

## Demonstrated

- canonical-only knowledge retention;
- exact source provenance through file SHA-256 and locator;
- machine-valid v2.1 JSON Schema;
- 75-record second pilot across nine engineering domains;
- 33/33 deterministic checks passed;
- benchmark and Jarvis capability metadata separated from canonical knowledge.

## Required before mapping all 11,000+ pages

1. Add typed `correlation_contract`: equation, coefficient source, property basis, units and validity range.
2. Add typed `graphical_method`: axes, construction steps, interpolation and outputs.
3. Add spreadsheet lineage: workbook hash, sheet, range, formula dependencies and unit cells.
4. Add FEM `verification_target`: quantity, location, aggregation, convergence behavior and tolerance source.
5. Add `knowledge_strength` to distinguish law, mechanistic model, empirical correlation and heuristic.
6. Freeze relation vocabulary and parent/decomposition rules.
7. Run a 150-record production rehearsal with two independent review passes and measure disagreement.

After these items, begin systematic mapping in bounded batches with quality sampling between batches.
